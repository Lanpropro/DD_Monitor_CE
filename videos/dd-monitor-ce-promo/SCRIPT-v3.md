# DD监控室CE 宣传片 · v3 制作规范

> **这份文档 = 你手写的 `Video_reference/Script.txt` + 补齐的执行规范 + 参考片实测。**
> 参考片：`Video_reference/来看看最新的_MC_启动器___LauncherX.29543304125.mp4`（1920×1080 / 24fps / **63.373s**）
> 标注约定：**[手稿]** 你的原话 · **[补齐]** 我补的执行细节 · **[实测]** 从参考片量出来的数字。

---

## 一、技术路线 [手稿]

**[手稿]** "抛弃视频的方式换用 html 和视频或动图素材实现模拟动效的视频效果"、
"没有素材的情况下你可以先留空标注"、"不同背景画面用 --- 隔开 要求动效连续
不同背景画面可用不同 html 文件 在剪辑上接续"

| 项 | 旧（v2，废弃） | 新（v3） |
| --- | --- | --- |
| 软件界面 | 4K 录屏裁切 | **HTML/CSS 按 `ddm/theme.py` 重绘** |
| 界面动效 | 依赖录屏里的真实操作 | **GSAP 前端补间**（可控、可寻帧） |
| 直播画面 | 录屏里的格子 | **视频 / 动图素材填格子**（未到位先留空） |
| 镜头运动 | 做不到 | **CSS 3D `perspective` + transform** |

**[补齐] 换的理由**：实测旧录屏里软件窗口有 **52% 是纯黑**（画面墙只占中间 42%），
且视频被提升到宿主根后无法参与镜头运动。

---

## 二、参考片实测 [实测]

用 HSV 直方图距离检测切点，63.37s 里 **40 个切点**，按段归纳：

| 段 | 时间 | 时长 | 主色 | 性质 |
| --- | --- | --- | --- | --- |
| 开场 | 0.00–6.58 | **6.58** | `#8d8283` `#524645` `#bd6e4f` | **暖棕+橙 · 实拍长镜头** |
| 快切 | 6.58–7.92 | 1.3 | 暖灰快速变化 | 转场 |
| 白底 A | 7.92–15.33 | 7.4 | **`#fcfcfb` 57→88%** | 白底 + 居中窗口 |
| **标题爆发** | 15.33–17.08 | **0.42 / 0.50 / 0.83** | `#545fcf` → `#fdac00` → `#fe0531` | **极快切（蓝紫→橙→红）** |
| 中灰 | 17.08–23.17 | 6.09 | `#abb0ba` `#8a8d9a` `#636473` | 卡片网格 |
| 白底 B | 23.17–28.75 | 5.6 | **`#f8f8f8` 79→94%** | 白底 |
| 深蓝 | 29.58–36.00 | 6.42 | **`#090c6d` 50.7%** + `#006c6b` | 深蓝 + 透视块 |
| 白底 C | 36.17–40.67 | 4.5 | `#eff0f3` `#f4f3f3` | 白底 |
| **紫** | 40.67–47.42 | 6.75 | **`#3801a5` 71%** `#823581` | 紫 |
| **白+橙** | 47.42–55.50 | **8.08** | `#fafafa` + **`#fcaa01` 23.8%** | 白底 + 橙色斜块 |
| 白底快切 | 55.50–60.00 | ~4.5 | 白底 + 蓝/橙 | 4 个 ~1.1s 场景 |
| **收尾** | 60.00–63.37 | 3.37 | **`#000000` 97.9%** | **纯黑定格** |

**从参考片学到的四条硬事实**：
1. **开场是 6.58s 纯氛围长镜头**，不是马上上标题 —— 先给氛围，再给信息。
2. **主背景是白底**（`#f8f8f8`–`#fefdfc`），不是深色；深色只在特定场景出现。
3. **有 0.25–0.5s 的极快切段**用于标题爆发（15.33–17.08s），平均镜头长被这段拉低。
4. **收尾是 3.37s 纯黑定格**，不是渐暗。

### 2.1 开场运动实测（0–4.96s）[实测]

逐帧相位相关（24fps；取画面 **30–60%** 带避开标题，用 **0–7%** 带交叉验证）：

| t | 相机累计位移 | 瞬时速度 | 同时发生 |
| --- | --- | --- | --- |
| 0.00–1.38 | 0 → −189px | 0 → −258px/s，**匀加速**（位移 ∝ t²） | 标题 **0.52s 起淡入** |
| 1.38–1.88 | −189px 不动 | ≈0（**静止 0.5s**） | **1.44s 软件窗口从画面右缘进入** |
| 1.79 → 2.04 | — | — | **标题隐去，0.25s 完成** |
| 1.92–4.00 | −202 → −857px | **恒定 −315px/s** | 长距离推移 |
| 4.00–4.96 | −857 → −1045px | 357 → 0 px/s（**减速收尾**） | 停住 |

两条必须注意的：

- 参考片在 **1.88s 处是原始素材的拼接点** —— 从静止一帧内跳到 315px/s，加速度物理上不成立。
  我们是一镜到底，照抄曲线形状但把起步做成平滑的（见第七节 01）。
- 参考片的软件窗口是**半透明玻璃**：背景的橙光**透过窗口**显示在窗口内部左上角。
  A 组「月光透过窗口」就是照这个做的（见第七节 04）。

**构图实测**（ASCII 还原）：白底场景是 **居中软件窗口，横向约 x 250–1632，下沿被画面裁掉**；
深蓝/白橙场景用 **斜向透视色块** 而不是平铺矩形。

---

## 三、硬规格 [手稿 + 补齐]

| 项 | 值 |
| --- | --- |
| 画幅 / 帧率 | **1920×1080 / 24fps** [手稿] |
| 总长 | **约 55.5s / 18 拍** [补齐：11–18 你留白了]（参考片 63.4s） |
| 声音 | 无口播 / 无配音 / 无口播字幕；BGM + 音效；**09 拍直播声出来时 BGM 停** [手稿] |
| 素材 | 没有素材处**先留空并显式标注** `[素材: 说明]` [手稿] |
| 文字 | 屏幕上只有下表写的那些字 [手稿] |

---

## 四、配色（**分两层** — 上一版最大的错就是把两层混成一层）

### 4.1 软件画面（格子 / 侧栏 / 弹层）→ 严格取自 `ddm/theme.py`

上一版拿 `#00a1d6` 亮蓝和 `#f1f2f5` 近白**铺满软件画面**，而软件里根本没有这两种大面积底色
—— 这就是"和软件不合、差距太大"的来源。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `BG` | `#222428` | 软件主背景（`mix(#2a2d32,#000,.20)`） |
| `SIDEBAR` | `#1d2023` | 侧栏 / 关注列表 |
| `CONTENT` | `#303338` | 卡片 / 输入框 |
| `ELEVATED` | `#373a3e` | 浮层 / 布局弹层 |
| `TILE_BG` | `#101114` | 直播格子底 |
| `ACCENT` | `#00a1d6` | **只作强调**：选中态 / 描边 / 按钮 / 模拟光标 |
| `PINK` | `#fb7299` | 直播徽标（全片唯一粉） |
| `TEXT1/2/3` | `#f1f2f5` / `rgba(214,218,224,.9)` / `rgba(184,189,197,.75)` | 文字三级 |
| `BORDER` | `rgba(131,131,145,.16)` | 1px 描边 |
| 圆角 / 侧栏宽 / 字体 | `4/8/12/16` · `248px` · `Microsoft YaHei UI` | 源码常量 |

### 4.2 场景色场（标题卡的背景）→ 取自参考片实测

| 名称 | 值 | 用在哪 | 参考片对应 |
| --- | --- | --- | --- |
| `sc-white` | `#f8f8f8` | **05 / 06 / 07 / 09 / 10 / 14–17**（主背景） | 白底 A/B/C |
| `sc-white-warm` | `#faf9f6` | 12 / 13 | 白+橙段 |
| `sc-deepblue` | `#090c6d` | 11（为啥不用浏览器） | 深蓝段 |
| `sc-violet` | `#3801a5` | 可选：15（反馈问题） | 紫段 |
| `sc-void` | `#0d0e10` | 01–04（视角段） | 开场 |
| `sc-black` | `#000000` | 18（收尾定格） | 收尾黑场 |
| `acc-orange` | `#fcaa01` | **强调色**：色块 / 数字 / 下划线 | 白+橙段 |

**规则**：白底场景上，**软件窗口仍是深灰的**（4.1 的令牌），深灰窗口落在白底上对比强、层次清楚
—— 这正是参考片的做法。**不要因为背景变白就把软件也刷白。**

---

## 五、动效规范 [补齐]

**只为这五个目的动**：反馈 / 空间连续性 / 状态指示 / 避免跳变 / 解释。

**属性**：只用 `transform`（`translate3d` / `scale` / `rotate` / `rotateY`）和 `opacity`。
**不动** `width/height/top/left/margin`（触发 layout，掉帧）。

**曲线**：
- 入场 / 退场 → **`cubic-bezier(0.23, 1, 0.32, 1)`**（强 ease-out）
- 屏上位移 / 变形 → `cubic-bezier(0.77, 0, 0.175, 1)`
- 匀速（滚动 / 弹幕）→ `linear`
- **摄像机位移 → `rail`（自定义）**：两端各 17% 做二次加减速，中间 66% 严格匀速。
  对称的 `ease-in-out` 在长距离移动里中段会"猛冲"，观感就是"快进缓出"；滑轨曲线没有这个问题
- **推进 / 落位 → `elastic.out(1, 0.62)`**：到位时要有一次看得见的回弹，别用干净的 `power*.out`
- **不用 `ease-in`**（起手慢，正卡在观众盯着的那一刻）

**时长**：

| 类型 | 时长 | 参考片依据 |
| --- | --- | --- |
| 元素飞入 / 填充 | 320–520ms | — |
| 布局切换（重排） | 480–720ms | — |
| 弹层弹出 | 180–260ms | — |
| **极快切（标题爆发）** | **250–500ms** | 15.33–17.08s 实测 0.42/0.50/0.83 |
| 光标移动 | 600–900ms | — |
| 镜头位移 / 推进 | 1200–2600ms | — |
| **开场氛围长镜头** | **6000ms 量级** | 0–6.58s 实测 6.58s |
| 收尾定格 | ≥3000ms | 60.00–63.37s 实测 3.37s |

**入场不用 `scale(0)`** —— 从 `scale(0.94~0.97) + opacity:0` 起。
**多元素入场错开 40–80ms**，不齐发。

---

## 六、文件划分 [补齐 — 落实"不同背景用不同 html"）

| # | 文件 | 拍 | 背景 |
| --- | --- | --- | --- |
| A | `18-01-intro.html` | 01–04 | `sc-void` + 视角右移 → 推进 |
| B | `18-02-layout.html` | 05–07 | **`sc-white`** |
| C | `18-03-hover.html` | 08 | 中灰（参考片 17–23s） |
| D | `18-04-audio.html` | 09–10 | `sc-white` |
| E | `18-05-why.html` | 11–13 | `sc-deepblue` |
| F | `18-06-oss.html` | 14–17 | `sc-white`（15 可选 `sc-violet`） |
| G | `18-07-end.html` | 18 | `sc-black` |

**接续要求**：相邻组的首帧 / 末帧在构图与色场上能对上。

---

## 七、逐拍规范

### 【0–12s】视角右移 + 软件填充 + 拆解（A 组 · `18-01-intro.html`）

**01 · 视角向右移动**（0.0–3.94s）
- **[手稿]** 视角向右移动；背景未定；左侧浮现出软件标题
- **[实现]** 背景 `bg-hall.jpg`（银河 + 山脉 + 湖面），在世界层 3600×1080 内 `object-fit: cover`；
  相机把世界层 `x` 从 0 推到 −1649（窗口落位居中）。曲线**形状**照参考片前 4s 实测（见 2.1），
  但按用户 2026-09-22 的反馈**去掉静止段并加快**：
  - `0.00–1.38s` **匀加速**（位移 ∝ t²，`power2.in`）走 189px，峰值 258px/s
  - `1.38–3.94s` **恒定 520px/s**（`none`）走 1330px —— **没有静止段**，加速一结束直接接上
  - `3.94–4.44s` **减速收尾**（`power2.out`）走 130px
  - 原样照抄参考片是「匀加速 → 静止 0.5s → 315px/s → 减速」；用户反馈「人眼看起来很慢」，
    所以删掉静止段、匀速从 315px/s 提到 520px/s，相机段总时长 6.10s → **4.44s**
- 软件窗口**平正**（照参考片那个窗口：正对镜头、无俯视倾斜）；世界坐标 `x=2109`，
  所以 `2109 − 189 = 1920` —— **窗口恰好 1.38s 贴上画布右缘进入**，而不是第 0 帧就露边
- 标题「DD监控室CE」+ 副标「多窗口直播监控」从 `translateX(-70px) + opacity:0` 浮入；
  **0.52s 起浮现**（550ms）、**1.79s 起 0.25s 隐去** —— 两个时刻都照参考片实测
- **[动效审核 2026-09-22]** glow / shadow 的淡入从 1.20s 收到 **0.60s**（原来比窗口晚 1s 才满，
  光晕是窗口的伴生效果，迟到会显得脱节）；新增 **`.sheen` 玻璃扫光**，窗口进入期间匀速扫过一次
  （`x 520 → −520`、`linear`、2.90s —— 相机右移，反射光斑相对窗口左移）；
  标题退场加 `filter: blur(2.5px)`（blur 遮住两态重叠时的双影）

**02 · 窗口以空壳进入**（随 01 一起落位）
- 软件窗口 = `Video_reference/fullscreen.png` 的真实截图切片（侧栏 / 主画面 / 5 小格，`--s: 0.2604`）
- **窗口先以空玻璃壳出现**（切片在这一拍还不可见，`opacity 0`）—— 参考片开场同样是窗口先到位、
  内容后出现；模块由 03 拍从下往上拼进来
- **窗口外壳 = 类玻璃材质**（照参考片 LauncherX 那个窗口）：圆角 14px + 1px 冷色亮棱边 +
  顶部内侧高光 + 底部内侧暗棱 + 表面 124° 斜向反光（`.appwin::after`）+ 四层柔和外阴影；
  底色是**高不透明度**深灰渐变（`.93→.96`）—— 切片缝隙**不透**背后背景，
  玻璃感只做在**边缘**，不靠底色透明（底色一透明，缝隙透出银河就又是"软件画面缺一块"的观感）
- 素材位：`[素材: 6 路直播画面 → 替换切片]`

**03 · 模块从下往上逐块拼合成窗口**（4.44–6.72s）
- **[手稿]** 视角突然向前推进；同背景；软件拆开成各元素
- **[用户 2026-09-22 改]** 不要"散开飞出"，改成**和参考片一样模块往上拼**（参考片 31.5/32.5/33.5s
  那种"新内容从下方推入、已有内容往上让位"的节奏）：
  窗口先以空玻璃壳出现在画面里，然后按**从下往上**的顺序
  —— 底部 3 格 → 右中格 → 顶部主画面 / 小格 → 侧栏 ——
  每块从窗口内下方 `y +120` 浮起落位。**节奏对齐 BGM**（用户 2026-09-22：请尝试匹配动效）：
  素材 `assets/bgm/ms-loop.wav` 的 onset 网格是 **0.1905s**（≈157.5 BPM 的八分音符，
  实测中位间隔 0.192s），所以 stagger = 0.1905s、每块 0.45s，
  起点 5.334s（= 网格上的点 0.1905 × 28），7 块在 6.93s 前落定
- **[动效审核 2026-09-22]** 入场补 `scale .97 → 1`：入场要有初始 transform 才有实体感
  —— 但**绝不用 `scale(0)`**（现实里没有东西是从"无"里冒出来的）
- **[实现]** 相机 4.44s 停稳后先做一次**弹性推进**（4.44s 起，`z 0→230`，`elastic.out(1, 0.62)`，0.96s），
  拼合从 5.40s 开始、6.72s 结束
- ⚠ **`.appwin` 现在必须写 `overflow:hidden`**：模块从窗口下方升起时否则会露在窗口外面
  （实测 t=5.7s 就是漏的）。以前禁止它，是因为它会剪掉"飞出画面"的 tween，而那个动画已经不存在了。
  加完用 `probe-shot.ps1` 数过像素，窗口下半部分没有被裁

**04 · 推进铺满 + 月光 + 标题**（7.00–12.0s）
- **[手稿]** 视角继续推进；各元素全部消散；出现标题 *向您介绍 **DDMonitorCE***
  （**用户 2026-09-22 覆盖了"消散"**：模块拼好后就留在窗口里，
  最后看到的就是"软件里模块的窗口"，不再留空壳）
- **[实现]** 相机 `z 230→1100`（1.5s，7.00s 起，`perspective 2000` → **约 2.22 倍**），
  窗口放大到**铺满画布**（2222×1216，宽高都盖过画布）。不能只用刚好 2.0 倍 ——
  窗口中心 y 是 509.5 而不是 540，2.0 倍时底部会差 23px 露出背景
- **背景图与暗角不再淡出**（用户要求「背景铺满画面」，银河湖面一直留到最后）
- **月光光影层 `.moon` 是 `.appwin` 的子层**（**8.382s** 起淡入 = 网格 0.1905 × 44）：**只有一层 `.wash` 冷白光晕**
  （径向渐变，光源在左上角）—— 用户 2026-09-22 要求**去掉星光光点**（`#moon i` / `.fl` 已删）；
  `inset:0 + overflow:hidden + border-radius` 把光限制在窗口内，透过窗口玻璃透出来 ——
  **光不直接显示在画面上，而是透过窗口**
- 主标题 **8.763s**（= 网格 0.1905 × 46）起从 `blur(7px)` **显影**到清晰（`opacity + y 26→0 + blur 7→0`，700ms）
- 8.763–12.0 **不再死停**：极缓推进 `z 1100 → 1130`（3.2s、`linear`、约 2.7%）——
  3.4s 的完全静止看起来像卡住了

### 【约 8–10s】布局演示

**05 · 想怎么看，就怎么排**（8.0–10.0s）
- **[手稿]** 视角不动；纯色背景；上方标题 *想怎么看 就怎么排*；下方软件页面
  **单屏换双屏换四分换1+5** [图片形式]
- **[补齐]** 背景 **`sc-white`**；下方软件窗口（深灰，横向 x 250–1632、下沿裁切，照参考片构图）；
  布局依次 `1x1 → 1x2 → 2x2 → corner`，每档 420ms 重排（格子真移动），末档停 400ms
- 素材位：`[素材: 6 路直播画面]`

### 【约 10–12s】弹幕

**06 · 弹幕跟着画面走**（10.0–12.0s）
- **[手稿]** 视角不动；同纯色背景；右侧标题 *弹幕跟着画面走*；左侧软件页面 **1+1+弹幕**；字体飞入后视角聚焦弹幕栏
- **[补齐]** 布局 `dm_main2`；标题从右 `translateX(40px)` 飞入；随后镜头 `scale 1.16` 聚焦弹幕栏并轻微平移；
  弹幕行 `linear` 连续上滚（**全片唯一持续运动**）
- **[用户 2026-09-22]** 软件页面**左移**（`x 250 → -250`，左侧 250px 被画面边缘遮掉），
  正好让出右侧给飞入的标题；**弹幕这一屏也换成真实截图**（由用户手动截取），
  我方只负责拼接与上滚动效 —— 到位前保留 `[素材: 弹幕画面]` 占位。
  标题留在**右上**（`top 60`）而不是原来的 `top 250`：那个位置和窗口重叠 372px，
  标题在 DOM 里先于窗口、会被整个盖住，成片里只剩右半截「着画面走」
- 素材位：`[素材: 1 路主画面 + 1 路小画面 + 弹幕文本]`

### 【约 12–14s】竖屏

**07 · 拖着竖的，他自己跟上**（12.0–14.0s）
- **[手稿]** 视角不动；同纯色背景；上方标题 *拖着竖的，他自己跟上* + *动态切换成竖屏，让你游戏之余，也能监控*；
  右侧飞入竖屏画面 **1+2 布局**；左侧是 06 的画面
- **[用户 2026-09-22 改]** 排版照他给的截图：**左侧横屏窗口落在左下、右侧竖屏窗口更高、
  两者底边对齐** —— 横屏 `scale .71` → box 约 `0,518,981x562`（左缘贴画面左边、下沿落 1080 贴底）；
  竖屏 `1243,300,430x780`（底边同样 1080，与横屏齐平；左缘距横屏右缘 262px）。
  **标题移到左上**（`left 110 / text-align left`）；副标「动态切换**成**竖屏」去掉"成"字
- **[补齐]** 左侧保留 06 定格；右侧竖屏窗口从画面外**右侧**滑入，比例 `16:9 → 9:16` 连续变形
  （`700x480 → 430x780`，1.04s），内部按 `portrait_main2`（主画面 16:9 整宽 + 2 小）重排
- 素材位：`[素材: 竖屏 1 主 + 2 小]`

### 【约 14–19s】悬停预览

**08 · 悬停预览 如此轻松**（14.0–21.5s，**7.5s**）
- **[手稿]** 视角不动；背景未定；多张卡片**立体排列**，**向上弹出 1 张**；模拟光标移到卡片上；
  播放直播预览；**重复 3 轮**；左上标题[立体效果] *悬停预览* + *如此轻松*
- **[用户 2026-09-22 改]** 手稿描述的「卡片栈」不对，画面照**参考片 17.08–23.17s**：
  背景是灰墙（从参考片 19.0s 裁出，用 `delogo` 抹掉了右上角 bilibili 水印）；
  **一排真实关注卡片从左下到右上排过去，左边在前、遮挡右边**（左近右远：左大右小）。
- **[用户 2026-09-23 改 · 其一]** 「按照截图样式重新摆放一下卡片，**不要动 3D 效果**，保持前后的透视关系，
  只是调整位置，让他在眼中看起来更倾斜」→ 只改 `POS`（x/y/scale/zIndex），
  **不加旋转、不改卡片尺寸、不动三段动效**。用户红框贴在 03 抽帧上，实测 6 个位置
  （标注图 2686x1511 → 1920x1080）：中心 x `333/647/940/1192/1436/1624`、
  y `845/781/740/704/646/576`；当时队列 5 张 → 6 张。
  中心间距 `313 → 188` 递密、落差 `36~69`，比原来的等距 326 / 落差 34 斜得多。
- **[用户 2026-09-23 改 · 其二]** 「将每张卡片放大为原来的 **1.5 倍**，多的部分删去，
  但是不要删除有录制素材的卡片」→ 卡片 `600x340 → **900x510**`（圆角 22→33、投影 32/64→48/96 同比）；
  900 宽在 1920 画面里只摆得下 **5 张**（中心间距 241.5，每张露出约 216px），
  所以删掉上一版最右上、没有录像的 `card-6`，**队列回到 5 张**；
  **有录像的 `#k1(card-2) / #k4(card-5) / #k5(card-7)` 一张不少**。
  中心 x `470/711.5/953/1194.5/1436`、y `795/731/690/654/596`
  （比上一版整体上移 50px —— 卡高了 170px，底部仍要留在画面内）；
  最左卡左缘 20、最右卡右缘 1785；`scale 1.000 → 0.776` 保持近大远小。
- **[用户 2026-09-23 改 · 其三]** 「要保留的三张是 莉蔻 盐无 和 四时小路」→
  **有录制素材的三张 = 莉蔻 / 盐无 / 四时小路**，卡面分别是
  `card-1(莉蔻Liko)` / `card-3(盐无Shio)` / `card-4(四时小路Komichi)`；
  它们摆在会被抽出的 **`#k1` / `#k4` / `#k5`**（三轮各抽一张、位置分散），各挂一段 `rec-preview.mp4`。
  没有录像的 `#k2` / `#k3` 用 `card-2(三理Mit3uri)` / `card-5(恩骨)` 补满
  （用户 2026-09-22：不是只留三张，摆得下就多摆）。
- **[用户 2026-09-23 改 · 其四]** 「① 将卡片的封面中心对齐，裁剪掉灰色区域；
  ② 对卡片做一点 3D 倾斜处理，类似截图；然后我们再来对齐视频素材」→
  ① 8 张 `card-*.png` 按 `297x182`（x 12–308）重裁，去掉左右各 12px 灰边、封面内容居中；
  ② 卡框按新比例 `297:182 = 1.632` 改成 `900x552`，`.stage` 给 `perspective: 2400`、
  每张卡 `rotationY: 12`（右缘向后 = 左近右远）；
  ③ 因为 3D 透视会放大靠近镜头的左半（静止时最左卡包围盒实测 918 宽），队列整体右移 30px。
  **视频素材的对齐留到下一轮**。
- **[用户 2026-09-23 改 · 其五]** 「3d 效果对了。把卡片的裁切完成，**下方还有灰边**；
  稍微缩小一点卡片，使第一张弹出时不会遮盖标题；卡片截图有点不太清晰，有没有办法修复一下」→
  重裁量到真实边界：源图 321x1561 里卡片内容 `x 12..308`、`y 13 起 / 两张之间 21 行纯背景 /
  周期 195 / 卡高 174`（旧参数 `x 12 / 高 182 / 周期 192 / 起点 26` 让每张卡底部多出 21 行灰边）；
  素材 297x174 用 Lanczos4 放到 **760x445** 再轻锐化（源只有 297 宽，放 3 倍必虚，只能这样拉回边缘）；
  卡框 `900x552 -> 760x445`；抽出上移量 `300 -> 250`（按卡高比例缩），第一张弹出时上缘 ≈281 > 标题底 258。
- **[用户 2026-09-23 改 · 其六]** 预览素材按卡分配（各裁 3s，760x428 @30fps）：
  liko → `preview-liko.mp4`（源 `聚焦卡片_卡片预览.mp4` 02–05s）；
  盐无 → `preview-shio.mp4`（13–16s）；四时小路 → `preview-komichi.mp4`（18–21s）。
- **[用户 2026-09-23 改 · 其七]** 「直播画面就是在**左边关注栏小窗**的画面，请裁剪；
  然后把背景换成 **01 的背景**，并且补上**同款的灯光和阴影**」→
  ① 原来裁的是整块软件界面（含侧栏/弹幕），不对。逐秒接触表 + 逐行亮度分析后确认：
     关注栏里那张卡会**展开成一块"卡片预览小窗"**（直播画面 + 弹幕）。
     三段各对应一张卡：**02–05s 在列表第 3 位（莉蔻）**、**13–16s 第 5 位（盐无）**、
     **18–21s 第 5 位（四时小路）**；小窗区域分别是 `230x135@(20,540)` / `196x115@(42,856)` /
     `205x120@(35,1016)`（4K 坐标，已按 1.707 比例取，避免变形），放大到 760x446。
     ⚠ 小窗在 4K 帧里只有 ~230px 宽，放大 3.3 倍，清晰度与卡片素材同级别，无法再高。
  ② 背景换成 01 那套：`assets/real/bg-hall.jpg`（object-position 50% 66%）+ `.vignette` 暗角；
     灯光 `.glow`（青色径向 `rgba(226,246,255,.46)` + `rgba(0,161,214,.26)`，blur 12px）与
     阴影 `.shadow`（黑色径向落地影，blur 14px）色值照抄 01，位置按 03 的卡片带重摆；
     两层跟着入场在 0.10s / 0.14s 淡入。
- **[用户 2026-09-23 改 · 其八]** 「将悬停播放的时长稍微延长一点，就可以尝试对齐 01/02 了」→
  预览 1.00 → **1.1905s（+1 拍）**、每轮 12 → **13 拍（2.4765s）**、整段 7.5 → **8.382s（44 拍）**；
  顺带修掉"片尾最后一张卡悬在半空"（旧版最后一轮 7.62s 才收完，而片段只有 7.5s）。
  连播：`v3-01-02-03-preview.mp4` = 01+02（带 BGM 前 18s）+ 新 03（BGM 接 18.0s）= **26.41s**。
  ⚠ 01+02 = 18.0s = **94.49 拍**，所以 03 的动作相对 BGM 节拍网格差约**半拍（0.095s）**；
    要完全贴合就把 03 的时间轴整体 +0.09525s。
  ⚠ 02 结尾是白场、03 开头是深色夜景 —— 目前是硬切，需要转场的话照 01→02 的白场做法再来一次。
- **[用户 2026-09-23 改 · 其九]** 「01 软件画面处的"[素材]"二字要删去，不止截图这一处」→
  逐处排查：01/02 的 HTML 与它们用到的**全部素材图**（7 张 `cell-*` + 4 张 `layout-*`）
  都没有这两个字；带 `[素材]` 的只有 `18-04-audio.html`（9 处）与 `18-06-oss.html`（9 处）的
  待补素材占位 —— **18 处已全部删掉**（占位块结构保留，将来填素材用）。
- **[用户 2026-09-23 改 · 其十]** 「14s 左右 02 的弹幕部分素材应该换用 `弹幕.mp4` 08s 开始的部分」→
  `assets/rec/rec-danmaku.mp4` 从原来的 `弹幕.mp4` 开头（四格布局）换成 **08s 起 5s**
  （才是 1+1+弹幕布局），1600x892 @30fps 同规格；02 与整片连播已重渲染。
- **[用户 2026-09-23 改 · 其十一]** 「删去 renders 文件夹中没用的部分」→
  `renders/` 只留当前成片（01/02/03 各 24+30fps、连播、3 张核对帧 + 1 张素材对照），
  临时抽帧（`_*`）、旧产物（`proto*` / `video*` / 04-07 旧渲染 / `v3-full`）全部清掉
  （200MB → 90MB）。
  ⚠ **教训**：这次连 `proto.mp4` / `proto-v3.mp4` / `video*.mp4` 一起删了（`Remove-Item` 不进回收站）。
  源文件都在所以重渲回来了（`proto.html` / `proto-v3.html` / `index.html`+`compositions/frames`），
  但**以后清理 renders 前先把"要删的清单"发给用户确认**。
- **[用户 2026-09-23 改 · 其十二]** 「照着脚本和参考片把后面的模板先补全」→ 04–07 四拍补全：
  ① `18-04-audio`（09/10 声道）：09 里 `[data-vol]` / `[data-aud]` 是**全局选择器**，三格会一起亮
     —— 改成按 `tiles[k]` 逐格；`left/top` 补间全改 `x/y`（宽高保留 CSS，见第十节）；
     10 的声波呼吸从 `scale` 改成音量条 `filter: brightness`（缩放会把格内画面/控制条一起拉变形）；
     补齐素材位与音频说明（BGM 淡出在混音层做）。
  ② `18-05-why`（11–13）原本已写完整，但 **13 段父层 `#l13` 从来没亮起来**，
     那 2.5s 是空蓝底 —— 已修（前后对比见 `renders/v3-05-why-fix.png`）。
  ③ `18-06-oss`（14–17）：`.wallwrap` 去掉 `transform-style: preserve-3d` 与 `will-change: transform`
     （第十节两个坑），3D 改由 `#root` 的 perspective + GSAP `transformPerspective` 承担；
     合缝不再补间 `gap`（布局属性会吸附整数像素），改成每格 `scale 1.014` 盖住那 6px 缝。
  ④ `18-07-end`（18）：本身完整（内嵌 logo SVG + 三层描边 + D 扫出 + CE 亮 + 缝合线），
     补了时长与"收黑在剪辑层"的分工说明。
  → 四拍各渲一版：**04 = 10.0s、05 = 7.5s、06 = 15.5s、07 = 3.5s**；
    **lint 从 3 个 error 降到 1 个**（只剩 `index.html` 的 `multiple_root_compositions`）。
    抽帧总览：`renders/v3-beats-04-07-check.png`。
  → 仍是模板状态（等素材）：04 的九格/两格、06 的九格画面墙都还是虚线占位；
    04 要的 3 路直播原声、12/13 的两个真实数值（M6）仍未提供。
- **[目标轮 · 素材接入 + lint 归零]** 把"素材位与已有素材接入齐备"补上：
  `rec-grid.mp4`（源 `9格布局.mp4`，1600x892）左侧 197px 是关注栏、右边正好 3×3 ——
  按 `467x297` 拆出 **9 张单格**（每格自带的控制条再裁掉 32px，免得与我方叠的控制条打架），
  存 `assets/rec/grid-1..9.png`；另从 `rec-chL/chR.mp4` 各取一帧（居中裁到 16:9）存 `chan-chl/chr.png`。
  → `18-04-audio`：9 格铺 `grid-1..9`，第 1/2 格各加一层 `.alt`（10 段 6.36s 切成左右声道画面）。
  → `18-06-oss`：画面墙 9 格铺 `grid-1..9`。
  → `proto.html` / `proto-v3.html` / `storyboard.html` 移进 **`prototypes/`**：
    它们与 `index.html` 同处根目录会触发 `multiple_root_compositions`（最后那个 error）。
  → **lint 归零（3 → 0 error，66 warning 都是既有项）**；04–07 各出 24fps + 30fps；
    抽帧总览 `renders/v3-beats-assets-check.png`。
  → 仍然缺：**M5 直播原声**（04 的 09/10）、**M6 两个真实数值**（12/13）。
- **[用户 2026-09-23 反馈 · 其十三]** 「02 的弹幕部分画面的右边下边有黑边，请裁剪」→
  量出来源 `弹幕.mp4`（4K）右边 **569px**、下边 **338px** 是黑边，内容实际 `x 0..3270 / y 0..1821`
  （比例 **1.795**，正好等于 02 窗口的 3271:1822）；重裁成 `crop=3270:1822:0:0` 再缩到 1600×892。
  顺手体检了其余录屏素材（`rec-grid` / `rec-portrait` / `rec-chL` / `rec-chR` / `rec-layout`
  / `preview-liko|shio|komichi` 与 4 张 `layout-*.png`）—— **都没有黑边**。
  02 与整片连播已重渲染（02 = 6.0s，连播 = 26.4s）。
- **[用户 2026-09-23 改 · 其十四 —— 03 整拍重构]** 「弹幕完全修改好了。让我们重构一下 03：
  `聚焦卡片_卡片预览.mp4` 要用的素材；**背景改为前面的纯色底**；将截图一的**左侧卡片栏取出**
  做一个**从下往上滑入**的动效；然后模拟鼠标**放置在上方的 3 个卡片**上展示预览；
  02→03 的过渡动效参考 skill 自己完成」→
  ① 从源 4.0s 帧裁出左侧关注栏（`x 0..380 × 全高`，380×2160）存 `assets/rec/follow-col.png`，
     显示为 253×1440，**从画面下方滑入**（y 620→0，0.74s `power3.out`，落定在 0.762 = 4 拍）。
  ② 背景换成 `sc-white #f8f8f8`（与 02 同底色），撤掉银河背景与卡片队列那套。
  ③ 悬停的三张卡 = 栏里**最上面三张开播中的卡**（粉色"直播中"标签 y = 820 / 987 / 1154
     → 莉蔻 / 盐无 / 四时小路）；每轮亮一个青色描边框（top 281 / 392 / 503，高 100），
     右侧预览窗显示**各自那一段**。
  ④ 02→03 过渡照 skill `transitions/overview.md` 的硬规矩「出场与入场同时」：
     02 的 `#scene` 从 5.40s 起 0.54s 上移 900px 滑出画面，03 的栏紧接着从下滑入，
     两拍共用一个纯白场（实测 02 末帧 bottom-half = **245.3** 纯白、03 首帧白底）。
  ⑤ ⚠ **素材坑**：三段预览各自的开头都有**黑场**（liko 1.13s / shio 0.37s / komichi 0.40s），
     已改成从源的 3.2 / 13.5 / 18.5s 起裁；并且**必须合成一支** `preview-hover.mp4`
     （liko 0–3 + shio 3–6 + komichi 6–9）—— 拆成三支 `<video>` 时渲染器管不过来：
     实测第三支要 7.2s 才有画面（设计 6.24s 淡入）、第一支 1.9s 才就绪，轮次之间还露黑底。
  → lint 仍 **0 error**（warning 66 → 63）；03 = 8.4167s（24fps）/ 8.4000s（30fps）；
    连播重拼 26.4s；核对图 `renders/v3-03-rebuild-check.png`。
    旧的 `v3-03-queue-check.png` / `-title-check.png` / `-preview-source.png` 已被这次重构取代删除。

  三轮各抽一张：**原位向上抬起 + 等比放大 1.22**（**不动 x、不提 zIndex** ——
  前后次序原样保留，所以左边更靠前的卡照样压住它；等比放大也不破坏透视）
  → 光标上移 → 卡面换成真实预览录像满 1s → 收回
- 时长 5s → **7.5s**（每轮 2.286s = 12 拍）；起点 0.762 / 3.048 / 5.334（0.1905s 网格）
- 素材：`card-2/5/7.png`（真实关注卡片）+ `rec-preview.mp4`（真实预览录像）；已就位
- **[用户 2026-09-23 改 · 其十五 —— 03 再改（其十四那版作废）]** 「我要**保留原本的软件画面**，
  模拟鼠标移动到卡片上、进行直播预览，这个时候**给上一个聚焦**就可以了」→
  ① 画面中央放**完整软件界面**：`assets/rec/focus-app.png` = `聚焦卡片_卡片预览.mp4`
     4.0s 帧的**整帧**（3840×2160；这一支满屏无黑边，不用裁），显示为 1540×866 居中。
  ② 鼠标在左侧关注栏里**从上往下依次移到三张开播中的卡**上 —— 4.0s 帧的列表是
     星言sumi / 灰泽满Hazel / [展开的预览小窗] / **三理Mit3uri / 盐无Shio / 四时小路Komichi** /
     恩骨 / 傲慢的小肉包 / 奶茶姬，三张用"直播中"粉色标签 y = 820 / 987 / 1154 定位。
  ③ 悬停那张卡**原地聚焦**：同一张图按 **3.6 倍**放大、用 `background-position` 裁到那张卡的范围，
     青色描边 + 投影 —— 不另开预览窗、不遮挡软件画面。**卡面本身就是那路直播，
     所以不需要额外的预览视频素材**（`preview-hover.mp4` 只服务于其十四那版"右侧预览窗"，已不用）。
     ⚠ 聚焦框按**左上角**对齐卡片左上角：卡片在画面最左边，按中心对齐会向左溢出软件画面。
  → lint 仍 **0 error**；03 = 8.4167s（24fps）/ 8.4000s（30fps）；连播重拼 26.4s；
    接缝复核：17.80s（02 滑出中）→ **18.00s 纯白 245.3** → 18.30s（03 淡入）→ 18.80s（稳定）。
- **[用户 2026-09-23 改 · 其十六 —— 03 第三次修正（其十五那版的"卡片原地放大"作废）]**
  「聚焦的话**将我们的视角推进**即可；要修改的是：这个时候的画面**停滞了、无法展示预览的效果**；
  并且**右侧和下侧还有黑边**，请去除」→
  ① **黑边**：`聚焦卡片_卡片预览.mp4` 的帧右边 **569px**、下边 **338px** 是黑的
     （与 `弹幕.mp4` 同源，内容区都是 `3270×1822`）→ 裁到内容边界后存 `focus-app.png`（3270×1822）。
  ② **聚焦 = 视角推进**：整层 `.stage` 一起 `scale 3.6` + 平移，把目标卡推到画面中心
     （探针 `tools/variants-03-focus.json` 实测 `#v1` box = `726,411 469×258` → 中心正好 (960,540)）。
  ③ **画面不再停滞**：推进到位后，那张卡的位置上**换成真实直播录像**
     （`preview-liko` / `preview-shio` / `preview-komichi`），动态可见。
  ④ **选帧改用 19.5s** —— 它的关注栏里莉蔻 / 盐无 / 四时小路三张都在
     （4.0s 帧最上面三张是三理Mit3uri / 盐无 / 四时小路，而三理Mit3uri 没有预览素材）；
     三张卡坐标：莉蔻 y 525..675、盐无 859..1009、四时小路 1193..1343（粉色标签 644/978/1312）。
  ⚠ 三支 `<video>` 各自**提前约 1.4s** 存在（data-start 0 / 2.24 / 4.72）——
    实测渲染器约需 1.1s 才能让一支视频出帧，卡着轮次起点给会得到黑画面。
  → lint **0 error**（65 warning）；03 = 8.4167s / 8.4000s；连播重拼 26.4s；
    核对图 `renders/v3-03-focus-check.png`（软件画面入场 → 三轮推进 + 三段实时录像）。
- **[用户 2026-09-23 反馈 · 其十七]** 「模拟鼠标位置出现问题了，请对准对应的位置」→
  **两个叠加的根因**：
  ① 光标原本放在 `.stage` **外面** —— 视角推进时卡片跟着走、光标留在原地，两者脱节；
  ② 把它移进 `.stage` 之后又被放大 3.6 倍，而 `scale` 默认以元素**中心**为原点，
     `1/ZOOM` 的反向缩放会把箭头尖端推偏（实测推进后光标 box = `977,570`，卡片中心是 `960,540`）。
  修正：`.cursor` 移进 `.stage` + `transform-origin: 0 0`（尖端位置与缩放无关）+ 偏移改 `cx-3 / cy-2`。
  探针复核（`tools/variants-03-cursor.json`）：**推进后**尖端 `(953,535)` vs 卡片中心 `(960,540)`；
  **未推进时** `(292,401)` vs `(291,401.5)` —— 两个状态都对准；
  抽帧特写见 `renders/v3-03-cursor-check.png`（三轮光标分别落在莉蔻 / 盐无 / 四时小路的画面上）。
- **[用户 2026-09-23 改 · 其十八]** 「将**指针和素材修改为上方三个卡片**的；
  并且在聚焦的时候**就不用收回来了，保持聚焦**，然后**视角下移**即可」→
  ① 三张卡改成截图里那三张：**莉蔻Liko / 三理Mit3uri / 盐无Shio**
     （就是 19.5s 帧左栏的第 3/4/5 张，卡片 y = 525 / 693 / 859），指针与卡内录像都跟着改。
     ⚠ **三理Mit3uri 没有专属录像** —— 暂时复用"红发少女"那段（`preview-komichi.mp4`，
     原本是四时小路的）；用户给时段我再换成专属素材。
  ② 动效改成**推近一次后不再退回**：第 1 轮把 stage 推到 3.6 倍并**保持**，
     第 2/3 轮只改 `tx/ty` 做**视角下移**，`scale` 全程 3.6 ——
     探针实测 stage 的 y：`-872 → -1161 → -1446`。每轮唯一淡入淡出的是卡内录像。
  → lint 0 error；03 = 8.4167s（24fps）/ 8.4000s（30fps）；连播重拼 26.4s；
    探针：三轮 `#v1/#v2/#v3` 依次落在 `(726,411) 469×258`（正好画面中心），光标始终 `(949,533)`。
- **[用户 2026-09-23 改 · 其十九 —— 03 改为直接用素材本体]** 「我来指定一下素材的对应：
  你只要完全把 `聚焦卡片_卡片预览.mp4` 素材 **0–15s** 放上来，做一个**加速、裁剪、聚焦**就好了」→
  ① **裁剪**：源 0–15s 裁掉右 569 / 下 338 黑边（内容 3270×1822）。
  ② **加速**：`setpts=PTS/1.7895` —— 15s → **8.37s**，正好对上 03 的 8.382s；缩到 **1560×870**。
     产物 `assets/rec/focus-clip.mp4`。
  ③ **聚焦**：主画面直接换成这支视频（不再往卡片上叠录像 —— 画面自己在动），
     聚焦沿用"推近一次到 3.6 倍后不退回、第 2/3 轮只做视角下移"。
  ⚠ **两个坑（都踩过）**：
     a) 删掉 `#v1/#v2/#v3` 后 JS 里还留着 `getElementById("v"+i).style...` → 抛 TypeError，
        整段脚本不执行。探针表现是 **"时间轴就绪: 否"**、stage 完全不动。
        **删元素时必须同步清掉 JS 里的引用。**
     b) `setpts` 加速把原关键帧间隔拉成 **8.33s**，渲染器报 **sparse keyframes**、
        seek 失败画面冻结。必须加 `-g 30 -keyint_min 30` 重编码（关键帧从 1 个变 8 个）。
  → lint **0 error**（63 warning）；03 = 8.4167s / 8.4000s；连播重拼 26.4s；
    探针：时间轴就绪，stage 6912×3888（= 3.6 倍），y `-868 → -1157 → -1442`（视角下移），光标 `(949,533)`。
- **[用户 2026-09-23 反馈 + 04 口述 · 其二十]**
  ① 「03 效果对了，就是感觉**有点糊**」→ 根因：素材原来缩到 **1560×870**，而聚焦要放大 3.6 倍
     （等效需要 5616px 宽的源，实际只有 3270）→ 素材改用**原始裁剪尺寸 3270×1822**（不再缩小），
     聚焦倍数降到 **2.0** —— `素材宽/显示宽 = 3270/1560 ≈ 2.096` 就是清晰上限。
  ② 「**让标题显现出来**」→ 03 的 `.head` 加 `z-index: 20`：`.stage` 在 DOM 里排在它后面，
     推进放大后会把标题整个盖住。
  ③ **04 的 10 段按口述重做**：「画面下移 → 标题在**画面中心放大** → 展示完标题变成
     *左右声道独立* → 画面**一分为二**（左=左声道 / 右=右声道），
     **它们要在展示前上移到中间位置**」：
     · `#h10a` 的 y 从 `-120`（还在顶部）改成 **`417`** —— 垂直居中：CSS top 62 + 417 = 479 ≈ 540-61；
     · `#appwin` 从 `y: 30`（top 292，偏下）改成 **`y: -107`** —— top 155 = (1080-770)/2，正中；
     · `.h` 加 **`z-index: 10`** + 一圈白色光晕：中心大标题会压到深色画面上，
       不给 z-index 会被 `.appwin` 盖住（实测 t=5.90 完全看不见），深字也读不出来。
  ④ 「**04 的素材我好像缺录了**，后面补给你，先用其他素材补上占位」→ 九格仍用 `grid-1..9.png`、
     左右声道仍用 `chan-chl/chr.png` 占位，等他补录再替换。
  → lint **0 error**；03 = 8.4167s / 8.4000s；04 = 10.0s（24fps）/ 10.0s（30fps）；
    核对图 `renders/v3-04-beat10-check.png`（中心大标题 → 1×2 居中 → 两格声道）。
- **[用户 2026-09-23 改 · 其二十一 —— 04 补录素材与双页纠正]**
  用户澄清「一分为二」是**两个完整软件页面并列**，不是单个软件窗口中的 1×2 布局。
  09 从 `声音打开.mp4` 的 9.4–11.1 / 14.4–16.1 / 19.4–21.0s 三段
  各取一次开声音操作，按原速接成 5.0s 的真实九格画面；10 先让九格页面下移、
  中心标题放大，再让 `左声道.mp4` 和 `右声道.mp4` 的**完整软件页面**从下方
  上移到中部。光标先指左页面的声道控制，再指右页面；两边依次显出对应声道状态。
  三段录屏均裁掉 OBS 右/下黑边，保留真实软件侧栏；音轨从补录文件提取。
- **[用户 2026-09-23 改 · 其二十二 —— 04 节奏与下方双页动效]**
  九格三次开声音改为每次约 **2.4s** 的原速录屏，逐格可听时间加长；
  「声音太杂乱听不清」抬到软件画面上方，避免压住画面。下方先出现**一套完整软件页面**，
  再从同一位置向左右分开为**两套完整软件页面**，不是软件内的 1×2 布局。
  左、右声道音轨改为前后播放，彼此不重叠；右声道单独播放 **2.5s**，
  右页选定后继续停留到片尾。本拍独立成片由 10.0s 延至 **15.5s**；
  后续拼接全片时，以新版 04 时长重排绝对时间码。
- **[用户 2026-09-23 改 · 其二十三 —— 左右同时播放与 01–04 连播]**
  04 的两个完整软件页面在分开后**同时打开、同时播放**，左声道只占左声道，
  右声道只占右声道，两个模拟光标同步点选；两条录屏音轨同起同止。
  `tools/assemble_v3_01_04.py` 使用 01/02/03/04 的 30fps HTML 渲染结果接成预览，
  01→02 淡接、02→03 淡黑、03→04 左滑；沿用 `assets/bgm/track.loop.mp3`，
  在直播原声出现时淡出音乐，在其前、两段原声之间及片尾无原声处淡入音乐；
  04 尾帧再停留 1.5s，让片尾音乐有完整的退场。连播预览约 **41.55s**。
- **[用户 2026-09-23 改 · 其二十四 —— 完成 05 并接入连播]**
  05 的平均缓冲时长和内存占用没有可比实测样本；用户允许改用其他数据。
  12 / 13 改为可由真实软件画面验证的功能文案：九路同屏、无需来回切标签、
  横竖布局按需调整。保留三段标题动效；`tools/assemble_v3_01_05.py`
  在 04→05 处增加淡黑过渡，配乐从 04 无原声段平滑接入 05。
- **[用户 2026-09-23 改 · 其二十五 —— 参考片文字与转场]**
  参照 `来看看最新的_MC_启动器___LauncherX.29543304125.mp4` 的 30–35s：
  05 改为左对齐的六行文字逐行出现、已出现的文字持续留在画面，
  深蓝底叠加缓慢移动的红、绿、紫色柔和渐变；保留已核实的功能文案。
  连播 24–25s（03→04）改为 0.7s 白闪切入九格软件画面。
- **[用户 2026-09-23 改 · 其二十六 —— 05 软件视觉]**
  删除「九路画面，同屏可见」一行；保留逐行累积的节奏。背景改用软件的深灰界面色，
  搭配青蓝与粉色光晕；右侧展示已有的软件总览画面、品牌标识和九宫格图形。
- **[用户 2026-09-23 改 · 其二十七 —— 03 预览清晰度]**
  对照原始 4K 录屏后确认悬停卡片本身仅占录屏的一小块；继续放大只会放大像素。
  `tools/build_v3_03_focus_clip.py` 保留原片三次悬停的顺序，在镜头移向下一张卡时
  跳过两处预览加载黑场，使用原生裁剪尺寸、温和锐化与更高编码质量。
  镜头推进从 2.0 倍调到 2.5 倍，让卡片稍大；第一次推进改为较快的 ease-out，
  后两次视角下移保持平滑，不另造一个放大的卡片层。
  03 场景用 delivery 画质渲染，01–05 连播的二次编码使用 CRF 16 保留细节。

### 【约 19–29s】声道

**09 · 听完你的听你的**（19.0–24.0s）
- **[手稿]** 视角不动；**直播声音出来时音乐停止**；同 05 纯色背景；上方标题 *听完你的听你的* + *各路独立左右声道路由*；
  下方软件 **9 分布局**；模拟光标**依次打开上方三个格子的声音**
- **[补齐]** 背景 `sc-white`；布局 `3x3`；光标依次移到左上三格的音量键（各约 2.4s），
  真实录屏展示音量操作；**此处 BGM 600ms 淡出、直播声进**（整片混音时处理）
- 素材位：`声音打开.mp4` 的九格画面及录屏音轨

**10 · 左右声道独立**（24.0–29.0s）
- **[手稿]** 视角不动；同背景；09 标题放大到画面中心 + 软件下移 → *声音太杂乱听不清* + *怎么办*；
  然后软件上移，布局**单画面一分为二左右**，标题上移 → *左右声道独立* + *我全部都要*；
  模拟光标将左侧画面打开左声道、右侧右声道
- **[补齐]** 三段落：①标题抬到软件上方并放大，九格软件下移，两者留出空隙；
  ②下方一套完整软件页面从下方出现，接着从同一位置分成左右两套，标题上移换文案；
  ③光标依次指向左、右页面的声道控制，分别显示「只播左声道」「只播右声道」，
  两条原声音轨分别占左/右声道、同时播放，片尾保留双页停留
- 素材位：`左声道.mp4`、`右声道.mp4` 的完整软件画面与音轨

### 【约 29–36.5s】三连问句

**11 · 为啥不用浏览器**（29.0–31.5s）
- **[手稿]** 视角不动；**另一纯色背景**；中心标题 *为啥不用浏览器*
- **[现版]** 软件深灰底上有缓慢移动的青蓝与粉色光晕；标题从左侧进入，右侧出现软件总览与品牌标识
- 素材位：无

**12 · 九路同屏**（31.5–34.0s）
- **[手稿]** 同背景；中心标题 *九路直播同开* + *超快的缓冲速度* + *平均时长约 xxx*
- **[现版]** 「九路直播同开」留在原位接续，加入「不用来回切标签」；右侧软件画面与九宫格图形辅助展示，不展示未经验证的时长
- 素材位：`[素材: 缓冲速度图示]`（可选）

**13 · 按需调整布局**（34.0–36.5s）
- **[手稿]** 同背景；中心标题 *九路直播同开* + *超低的内存占用* + *平均占用约 xxx*
- **[现版]** 同 12 结构；下方逐行加入「按需调整布局」和「横竖画面，自由摆放」；不展示未经验证的内存占用
- 素材位：`[素材: 内存占用图示]`（可选）

### 06–07 新版脚本：录屏跟随、及时重放、同页信息、画面墙与结尾

> 以 [`SCRIPT-06-RECORDED.md`](SCRIPT-06-RECORDED.md) 为 06–07 的现行分镜。06 暂定 27.4 秒，07 沿用 3.5 秒；现有 `18-06-oss.html` 和连播预览尚未按新版脚本制作。

- 06 开场：1+5 布局中依次点开六格录屏，镜头按真实点击顺序聚焦并跟随，之后退远脱离软件窗口。
- 及时重放：PR 页面实录里两段长短不同的素材，用播放头定位和对应预览说明精确找到目标画面。
- 信息页：「反馈问题」与「关于项目」在同一版面展开；右侧同心圆弧持续缓慢运动。
- 画面墙：多段不同九格实录各取中央 3×3，上下拼接成动态长墙，从画面下方上抽；末尾收黑。
- 07：沿用官方 logo 拼装与字标定格。

---

## 八、素材清单（待你提供）

| # | 素材 | 用在哪 | 规格 | 状态 |
| --- | --- | --- | --- | --- |
| M1 | 直播画面 ×6 | 02 / 05 / 06 / 07 | 6–15s，16:9，可循环 | ✅ 录屏已含（见 8.1，可裁单格） |
| M2 | 直播画面 ×9 | 09 / 17 | 同上（可与 M1 复用） | ✅ 录屏已含（见 8.1） |
| M3 | 弹幕画面 | 06 | 用户手动截取 | ✅ 已提供 `弹幕.mp4`（1. + 1 + 弹幕布局，20.38s，4K60） |
| M4 | 关注卡片封面 + 直播预览 | 08 | 手稿要 3 轮 | ✅ 已提供 `聚焦卡片_卡片预览.mp4`（22.78s，4K60）+ `卡片截图_自行裁剪.png`（321×1561，**需自行裁剪**） |
| M5 | 直播原声 | 09 / 10 | 补录三段的录屏音轨 | ✅ 已从 `声音打开.mp4` / `左声道.mp4` / `右声道.mp4` 提取 |
| M6 | 缓冲时长 / 内存占用数值 | 12 / 13 | 两个真实数字 | 用户允许改为可验证的功能文案，现版无需数值 |
| M7 | 1+5 逐格点开录屏 | 06 开场 | 完整窗口、六次点击与画面变化 | 待提供 |
| M11 | PR 页面及时重放录屏 | 06 中段 | 同一时间轨有长短不同的两段素材、定位与播放过程 | 待提供 |
| M12 | 不同九格布局软件录屏 | 06 画面墙 | 至少两段，可裁中央 3×3，画面持续运动 | 待提供 |
| M8 | 参考链接 | 03 | 你手稿写了"参考 xxx" | **待提供** |
| M9 | BGM + 音效 | 全片 | 已有 | 已有 |
| M10 | 官方 logo | 18 | 已有 `assets/logo.svg` | 已有 |

### 8.1 录屏素材（用户 2026-09-22 23:08 提供，都在 `Video_reference/`）

五段都是 **3840×2160 / 60fps**、整块软件窗口的录屏（含侧栏），可以直接缩放当窗口内容用，
不必再裁。**动效仍归我方**：录屏里已有的操作照播，标题 / 镜头 / 节奏由 composition 叠加。

| 文件 | 时长 | 抽帧看到的内容 | 对应拍 | 状态 |
| --- | --- | --- | --- | --- |
| `布局切换.mp4` | 30.48s | 1x1 → 1x2 → 2x2 → 1+5 逐档真切换 | 05 | ✅ 可用 |
| `9格布局.mp4` | 10.53s | 9 分布局（3×3 九路） | 09 | ✅ 可用 |
| `切换竖屏.mp4` | 22.72s | 多格 → 竖屏窗口出现 → 单画面；**开头有一段黑场** | 07 | ⚠️ 要定位竖屏段 |
| `左声道.mp4` | 13.80s | 1x1 单画面（DD 监控室，左侧带弹幕） | 10 | ✅ 可用 |
| `右声道.mp4` | 13.93s | 1x1 单画面（用户已人工确认可用） | 10 | ✅ 可用 |
| `弹幕.mp4` | 20.38s | **1+1+弹幕布局**（06 要的就是这个） | 06 | ✅ 已接入 02 |
| `聚焦卡片_卡片预览.mp4` | 22.78s | 卡片预览（08 用） | 08 | ✅ 已提供 |
| `卡片截图_自行裁剪.png` | 321×1561 | 卡片截图，**需自行裁剪** | 08 | ✅ 已提供 |

**已接入 02 的轻量副本**（4K/60fps 原件 → 窗口尺寸 30fps，`assets/rec/`，113MB → 2.2MB）：
`layout-1x1/1x2/2x2/corner.png`（05 四档布局，从 `布局切换.mp4` 的 6/13/18/24s 裁出）、
`rec-danmaku.mp4`（06）、`rec-portrait.mp4`（07）。

**仍然缺的**：新版 06 的 1+5 点击录屏、PR 页面及时重放录屏、多段九格录屏（M7 / M11 / M12）。12 / 13 的数值已按用户授权改为功能文案（M6 现版无需）。

---

## 九、待你确认

1. **03 的"参考 xxx"** 是什么？（手稿没写完）
2. ~~**12 / 13 的两个数值**~~ → 用户允许更换内容，现版已改为九路同屏和布局适配。
3. **06 新录屏**：点击顺序、时间轨定位点与九格裁切边界在素材到位后逐帧确认。
4. **总长**：06 改为暂定 27.4s，07 为 3.5s；全片时长在素材到位后按实际操作节奏重算。
5. **场景色场**：我按参考片定了"白底为主 + 11 用深蓝 + 18 黑"。**要不要保留你没提的紫段？**
6. ~~**01/02 的"背景未定"**~~ → 已定：`assets/real/bg-hall.jpg`（银河 + 山脉 + 湖面，取自 `Video_reference/background.jpg`）。

---

## 十、渲染陷阱（实测踩过的坑，写新镜头前必读）

这些坑的共同点是：DOM 的 `getBoundingClientRect()` 完全正常，但**绘制**是错的。
凡涉及 3D 变换，必须用 `tools/probe-variants.mjs` 截图后数像素来验证，只看几何会被骗。

| 症状 | 根因 | 正确做法 |
| --- | --- | --- |
| 软件窗口只画出上半部分，下半部分连窗口底色都没有，直接透出背景 | 祖先层有 `transform-style: preserve-3d`，子层再做 `rotationX` / `z` 变换时 Chrome 把该子层的绘制裁到局部 y≈363（实测只剩 417/548 行） | 祖先层**不要** `preserve-3d`；立体感改成给每个 3D 元素自带 `transformPerspective`（GSAP 属性），效果等价 |
| 同上 | 参与 3D 的层写了 `will-change: transform` —— Chrome 按布局尺寸给合成层光栅化，投影超出该边界就被整块裁掉 | 这些层不要写 `will-change` |
| 拆解"飞出画面"的模块看起来在原地消失 | 父层 `overflow: hidden` 把飞出动画整块剪掉了 | 承载"飞出去"的父层不要裁切，圆角交给子元素自己 `border-radius` |
| 布局动画一顿一顿 | 补间了 `left` / `top`，布局属性会吸附到整数设备像素 | 位移用 `x` / `y`；宽高可以保留 CSS 补间（离线逐帧渲染不在乎 layout 成本，而 transform 缩放会把格子里的文字/控制条一起拉变形） |
| 时间轴上的 callback 不触发 | 渲染器用 `seek(t, suppressEvents=true)` 跳帧取画 | 所有状态变化都必须写成时间轴上的补间，不能用 `tl.call()` |
| 相邻 tween 抢同一个 transform 分量，中途跳变 | 两个 tween 都补 `x` 且开始时间错开 | 让它们同时开始、各补不同属性（一个补 `opacity`，一个补 `width/height/x/y`） |

**验证工具**（都在 `tools/`）：

- `probe-variants.mjs` + 一份 JSON 配置 → 在不同时刻各截一张图，并打印选择器的真实 `opacity` 与 `getBoundingClientRect`
- `probe-shot.ps1` → 在截图上数某一列的暗行数、找"暗→亮"的裁切边界。**模型看不到图，构图只能靠这个数出来**
- `probe-dom.mjs` → 静态服务器 + CDP，把时间轴 seek 到指定时刻后读真实几何（注意 Windows 上路径分隔符要统一成 `/`，否则资源全 404，几何全是未变换的假象）
- `.ps1` 一律**纯 ASCII 注释**：Windows PowerShell 5.1 按 ANSI 读脚本，UTF-8 中文注释会变乱码，可能破坏字符串字面量
