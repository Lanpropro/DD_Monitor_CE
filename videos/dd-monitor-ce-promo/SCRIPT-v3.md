# DD监控室CE 宣传片 · v3 制作规范

> **这份文档 = 你手写的 `Video_reference/Script.txt` + 补齐的执行规范 + 参考片实测。**
> 参考片：`Video_reference/来看看最新的_MC_启动器___LauncherX.29543304125.mp4`（1920×1080 / 24fps / **63.373s**）
> 标注约定：**[手稿]** 你的原话 · **[补齐]** 我补的执行细节 · **[实测]** 从参考片量出来的数字。

---

## 一、技术路线 [手稿]

**[手稿]** "抛弃视频的方式换用 html 和视频或动图素材实现模拟动效的视频效果"、
"没有素材的情况下你可以先留空标注"、"不同背景画面用 --- 隔开 要求动效连续
不同背景画面可用不同 html 文件 在剪辑上接续"

| 项 | 旧（v2，废弃） | 新（v3） |
| --- | --- | --- |
| 软件界面 | 4K 录屏裁切 | **HTML/CSS 按 `ddm/theme.py` 重绘** |
| 界面动效 | 依赖录屏里的真实操作 | **GSAP 前端补间**（可控、可寻帧） |
| 直播画面 | 录屏里的格子 | **视频 / 动图素材填格子**（未到位先留空） |
| 镜头运动 | 做不到 | **CSS 3D `perspective` + transform** |

**[补齐] 换的理由**：实测旧录屏里软件窗口有 **52% 是纯黑**（画面墙只占中间 42%），
且视频被提升到宿主根后无法参与镜头运动。

---

## 二、参考片实测 [实测]

用 HSV 直方图距离检测切点，63.37s 里 **40 个切点**，按段归纳：

| 段 | 时间 | 时长 | 主色 | 性质 |
| --- | --- | --- | --- | --- |
| 开场 | 0.00–6.58 | **6.58** | `#8d8283` `#524645` `#bd6e4f` | **暖棕+橙 · 实拍长镜头** |
| 快切 | 6.58–7.92 | 1.3 | 暖灰快速变化 | 转场 |
| 白底 A | 7.92–15.33 | 7.4 | **`#fcfcfb` 57→88%** | 白底 + 居中窗口 |
| **标题爆发** | 15.33–17.08 | **0.42 / 0.50 / 0.83** | `#545fcf` → `#fdac00` → `#fe0531` | **极快切（蓝紫→橙→红）** |
| 中灰 | 17.08–23.17 | 6.09 | `#abb0ba` `#8a8d9a` `#636473` | 卡片网格 |
| 白底 B | 23.17–28.75 | 5.6 | **`#f8f8f8` 79→94%** | 白底 |
| 深蓝 | 29.58–36.00 | 6.42 | **`#090c6d` 50.7%** + `#006c6b` | 深蓝 + 透视块 |
| 白底 C | 36.17–40.67 | 4.5 | `#eff0f3` `#f4f3f3` | 白底 |
| **紫** | 40.67–47.42 | 6.75 | **`#3801a5` 71%** `#823581` | 紫 |
| **白+橙** | 47.42–55.50 | **8.08** | `#fafafa` + **`#fcaa01` 23.8%** | 白底 + 橙色斜块 |
| 白底快切 | 55.50–60.00 | ~4.5 | 白底 + 蓝/橙 | 4 个 ~1.1s 场景 |
| **收尾** | 60.00–63.37 | 3.37 | **`#000000` 97.9%** | **纯黑定格** |

**从参考片学到的四条硬事实**：
1. **开场是 6.58s 纯氛围长镜头**，不是马上上标题 —— 先给氛围，再给信息。
2. **主背景是白底**（`#f8f8f8`–`#fefdfc`），不是深色；深色只在特定场景出现。
3. **有 0.25–0.5s 的极快切段**用于标题爆发（15.33–17.08s），平均镜头长被这段拉低。
4. **收尾是 3.37s 纯黑定格**，不是渐暗。

### 2.1 开场运动实测（0–4.96s）[实测]

逐帧相位相关（24fps；取画面 **30–60%** 带避开标题，用 **0–7%** 带交叉验证）：

| t | 相机累计位移 | 瞬时速度 | 同时发生 |
| --- | --- | --- | --- |
| 0.00–1.38 | 0 → −189px | 0 → −258px/s，**匀加速**（位移 ∝ t²） | 标题 **0.52s 起淡入** |
| 1.38–1.88 | −189px 不动 | ≈0（**静止 0.5s**） | **1.44s 软件窗口从画面右缘进入** |
| 1.79 → 2.04 | — | — | **标题隐去，0.25s 完成** |
| 1.92–4.00 | −202 → −857px | **恒定 −315px/s** | 长距离推移 |
| 4.00–4.96 | −857 → −1045px | 357 → 0 px/s（**减速收尾**） | 停住 |

两条必须注意的：

- 参考片在 **1.88s 处是原始素材的拼接点** —— 从静止一帧内跳到 315px/s，加速度物理上不成立。
  我们是一镜到底，照抄曲线形状但把起步做成平滑的（见第七节 01）。
- 参考片的软件窗口是**半透明玻璃**：背景的橙光**透过窗口**显示在窗口内部左上角。
  A 组「月光透过窗口」就是照这个做的（见第七节 04）。

**构图实测**（ASCII 还原）：白底场景是 **居中软件窗口，横向约 x 250–1632，下沿被画面裁掉**；
深蓝/白橙场景用 **斜向透视色块** 而不是平铺矩形。

---

## 三、硬规格 [手稿 + 补齐]

| 项 | 值 |
| --- | --- |
| 画幅 / 帧率 | **1920×1080 / 24fps** [手稿] |
| 总长 | **约 55.5s / 18 拍** [补齐：11–18 你留白了]（参考片 63.4s） |
| 声音 | 无口播 / 无配音 / 无口播字幕；BGM + 音效；**09 拍直播声出来时 BGM 停** [手稿] |
| 素材 | 没有素材处**先留空并显式标注** `[素材: 说明]` [手稿] |
| 文字 | 屏幕上只有下表写的那些字 [手稿] |

---

## 四、配色（**分两层** — 上一版最大的错就是把两层混成一层）

### 4.1 软件画面（格子 / 侧栏 / 弹层）→ 严格取自 `ddm/theme.py`

上一版拿 `#00a1d6` 亮蓝和 `#f1f2f5` 近白**铺满软件画面**，而软件里根本没有这两种大面积底色
—— 这就是"和软件不合、差距太大"的来源。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `BG` | `#222428` | 软件主背景（`mix(#2a2d32,#000,.20)`） |
| `SIDEBAR` | `#1d2023` | 侧栏 / 关注列表 |
| `CONTENT` | `#303338` | 卡片 / 输入框 |
| `ELEVATED` | `#373a3e` | 浮层 / 布局弹层 |
| `TILE_BG` | `#101114` | 直播格子底 |
| `ACCENT` | `#00a1d6` | **只作强调**：选中态 / 描边 / 按钮 / 模拟光标 |
| `PINK` | `#fb7299` | 直播徽标（全片唯一粉） |
| `TEXT1/2/3` | `#f1f2f5` / `rgba(214,218,224,.9)` / `rgba(184,189,197,.75)` | 文字三级 |
| `BORDER` | `rgba(131,131,145,.16)` | 1px 描边 |
| 圆角 / 侧栏宽 / 字体 | `4/8/12/16` · `248px` · `Microsoft YaHei UI` | 源码常量 |

### 4.2 场景色场（标题卡的背景）→ 取自参考片实测

| 名称 | 值 | 用在哪 | 参考片对应 |
| --- | --- | --- | --- |
| `sc-white` | `#f8f8f8` | **05 / 06 / 07 / 09 / 10 / 14–17**（主背景） | 白底 A/B/C |
| `sc-white-warm` | `#faf9f6` | 12 / 13 | 白+橙段 |
| `sc-deepblue` | `#090c6d` | 11（为啥不用浏览器） | 深蓝段 |
| `sc-violet` | `#3801a5` | 可选：15（反馈问题） | 紫段 |
| `sc-void` | `#0d0e10` | 01–04（视角段） | 开场 |
| `sc-black` | `#000000` | 18（收尾定格） | 收尾黑场 |
| `acc-orange` | `#fcaa01` | **强调色**：色块 / 数字 / 下划线 | 白+橙段 |

**规则**：白底场景上，**软件窗口仍是深灰的**（4.1 的令牌），深灰窗口落在白底上对比强、层次清楚
—— 这正是参考片的做法。**不要因为背景变白就把软件也刷白。**

---

## 五、动效规范 [补齐]

**只为这五个目的动**：反馈 / 空间连续性 / 状态指示 / 避免跳变 / 解释。

**属性**：只用 `transform`（`translate3d` / `scale` / `rotate` / `rotateY`）和 `opacity`。
**不动** `width/height/top/left/margin`（触发 layout，掉帧）。

**曲线**：
- 入场 / 退场 → **`cubic-bezier(0.23, 1, 0.32, 1)`**（强 ease-out）
- 屏上位移 / 变形 → `cubic-bezier(0.77, 0, 0.175, 1)`
- 匀速（滚动 / 弹幕）→ `linear`
- **摄像机位移 → `rail`（自定义）**：两端各 17% 做二次加减速，中间 66% 严格匀速。
  对称的 `ease-in-out` 在长距离移动里中段会"猛冲"，观感就是"快进缓出"；滑轨曲线没有这个问题
- **推进 / 落位 → `elastic.out(1, 0.62)`**：到位时要有一次看得见的回弹，别用干净的 `power*.out`
- **不用 `ease-in`**（起手慢，正卡在观众盯着的那一刻）

**时长**：

| 类型 | 时长 | 参考片依据 |
| --- | --- | --- |
| 元素飞入 / 填充 | 320–520ms | — |
| 布局切换（重排） | 480–720ms | — |
| 弹层弹出 | 180–260ms | — |
| **极快切（标题爆发）** | **250–500ms** | 15.33–17.08s 实测 0.42/0.50/0.83 |
| 光标移动 | 600–900ms | — |
| 镜头位移 / 推进 | 1200–2600ms | — |
| **开场氛围长镜头** | **6000ms 量级** | 0–6.58s 实测 6.58s |
| 收尾定格 | ≥3000ms | 60.00–63.37s 实测 3.37s |

**入场不用 `scale(0)`** —— 从 `scale(0.94~0.97) + opacity:0` 起。
**多元素入场错开 40–80ms**，不齐发。

---

## 六、文件划分 [补齐 — 落实"不同背景用不同 html"）

| # | 文件 | 拍 | 背景 |
| --- | --- | --- | --- |
| A | `18-01-intro.html` | 01–04 | `sc-void` + 视角右移 → 推进 |
| B | `18-02-layout.html` | 05–07 | **`sc-white`** |
| C | `18-03-hover.html` | 08 | 中灰（参考片 17–23s） |
| D | `18-04-audio.html` | 09–10 | `sc-white` |
| E | `18-05-why.html` | 11–13 | `sc-deepblue` → `sc-white-warm` |
| F | `18-06-oss.html` | 14–17 | `sc-white`（15 可选 `sc-violet`） |
| G | `18-07-end.html` | 18 | `sc-black` |

**接续要求**：相邻组的首帧 / 末帧在构图与色场上能对上。

---

## 七、逐拍规范

### 【0–12s】视角右移 + 软件填充 + 拆解（A 组 · `18-01-intro.html`）

**01 · 视角向右移动**（0.0–3.94s）
- **[手稿]** 视角向右移动；背景未定；左侧浮现出软件标题
- **[实现]** 背景 `bg-hall.jpg`（银河 + 山脉 + 湖面），在世界层 3600×1080 内 `object-fit: cover`；
  相机把世界层 `x` 从 0 推到 −1649（窗口落位居中）。曲线**形状**照参考片前 4s 实测（见 2.1），
  但按用户 2026-09-22 的反馈**去掉静止段并加快**：
  - `0.00–1.38s` **匀加速**（位移 ∝ t²，`power2.in`）走 189px，峰值 258px/s
  - `1.38–3.94s` **恒定 520px/s**（`none`）走 1330px —— **没有静止段**，加速一结束直接接上
  - `3.94–4.44s` **减速收尾**（`power2.out`）走 130px
  - 原样照抄参考片是「匀加速 → 静止 0.5s → 315px/s → 减速」；用户反馈「人眼看起来很慢」，
    所以删掉静止段、匀速从 315px/s 提到 520px/s，相机段总时长 6.10s → **4.44s**
- 软件窗口**平正**（照参考片那个窗口：正对镜头、无俯视倾斜）；世界坐标 `x=2109`，
  所以 `2109 − 189 = 1920` —— **窗口恰好 1.38s 贴上画布右缘进入**，而不是第 0 帧就露边
- 标题「DD监控室CE」+ 副标「多窗口直播监控」从 `translateX(-70px) + opacity:0` 浮入；
  **0.52s 起浮现**（550ms）、**1.79s 起 0.25s 隐去** —— 两个时刻都照参考片实测
- **[动效审核 2026-09-22]** glow / shadow 的淡入从 1.20s 收到 **0.60s**（原来比窗口晚 1s 才满，
  光晕是窗口的伴生效果，迟到会显得脱节）；新增 **`.sheen` 玻璃扫光**，窗口进入期间匀速扫过一次
  （`x 520 → −520`、`linear`、2.90s —— 相机右移，反射光斑相对窗口左移）；
  标题退场加 `filter: blur(2.5px)`（blur 遮住两态重叠时的双影）

**02 · 窗口以空壳进入**（随 01 一起落位）
- 软件窗口 = `Video_reference/fullscreen.png` 的真实截图切片（侧栏 / 主画面 / 5 小格，`--s: 0.2604`）
- **窗口先以空玻璃壳出现**（切片在这一拍还不可见，`opacity 0`）—— 参考片开场同样是窗口先到位、
  内容后出现；模块由 03 拍从下往上拼进来
- **窗口外壳 = 类玻璃材质**（照参考片 LauncherX 那个窗口）：圆角 14px + 1px 冷色亮棱边 +
  顶部内侧高光 + 底部内侧暗棱 + 表面 124° 斜向反光（`.appwin::after`）+ 四层柔和外阴影；
  底色是**高不透明度**深灰渐变（`.93→.96`）—— 切片缝隙**不透**背后背景，
  玻璃感只做在**边缘**，不靠底色透明（底色一透明，缝隙透出银河就又是"软件画面缺一块"的观感）
- 素材位：`[素材: 6 路直播画面 → 替换切片]`

**03 · 模块从下往上逐块拼合成窗口**（4.44–6.72s）
- **[手稿]** 视角突然向前推进；同背景；软件拆开成各元素
- **[用户 2026-09-22 改]** 不要"散开飞出"，改成**和参考片一样模块往上拼**（参考片 31.5/32.5/33.5s
  那种"新内容从下方推入、已有内容往上让位"的节奏）：
  窗口先以空玻璃壳出现在画面里，然后按**从下往上**的顺序
  —— 底部 3 格 → 右中格 → 顶部主画面 / 小格 → 侧栏 ——
  每块从窗口内下方 `y +120` 浮起落位（stagger 80ms、0.75s 一块、`EASE_OUT`）
- **[动效审核 2026-09-22]** 入场补 `scale .97 → 1`：入场要有初始 transform 才有实体感
  —— 但**绝不用 `scale(0)`**（现实里没有东西是从"无"里冒出来的）
- **[实现]** 相机 4.44s 停稳后先做一次**弹性推进**（4.44s 起，`z 0→230`，`elastic.out(1, 0.62)`，0.96s），
  拼合从 5.40s 开始、6.72s 结束
- ⚠ **`.appwin` 现在必须写 `overflow:hidden`**：模块从窗口下方升起时否则会露在窗口外面
  （实测 t=5.7s 就是漏的）。以前禁止它，是因为它会剪掉"飞出画面"的 tween，而那个动画已经不存在了。
  加完用 `probe-shot.ps1` 数过像素，窗口下半部分没有被裁

**04 · 推进铺满 + 月光 + 标题**（7.00–12.0s）
- **[手稿]** 视角继续推进；各元素全部消散；出现标题 *向您介绍 **DDMonitorCE***
  （**用户 2026-09-22 覆盖了"消散"**：模块拼好后就留在窗口里，
  最后看到的就是"软件里模块的窗口"，不再留空壳）
- **[实现]** 相机 `z 230→1100`（1.5s，7.00s 起，`perspective 2000` → **约 2.22 倍**），
  窗口放大到**铺满画布**（2222×1216，宽高都盖过画布）。不能只用刚好 2.0 倍 ——
  窗口中心 y 是 509.5 而不是 540，2.0 倍时底部会差 23px 露出背景
- **背景图与暗角不再淡出**（用户要求「背景铺满画面」，银河湖面一直留到最后）
- **月光光影层 `.moon` 是 `.appwin` 的子层**（8.30s 起淡入）：**只有一层 `.wash` 冷白光晕**
  （径向渐变，光源在左上角）—— 用户 2026-09-22 要求**去掉星光光点**（`#moon i` / `.fl` 已删）；
  `inset:0 + overflow:hidden + border-radius` 把光限制在窗口内，透过窗口玻璃透出来 ——
  **光不直接显示在画面上，而是透过窗口**
- 主标题 8.60s 起从 `blur(7px)` **显影**到清晰（`opacity + y 26→0 + blur 7→0`，700ms）
- 8.60–12.0 **不再死停**：极缓推进 `z 1100 → 1130`（3.4s、`linear`、约 2.7%）——
  3.4s 的完全静止看起来像卡住了

### 【约 8–10s】布局演示

**05 · 想怎么看，就怎么排**（8.0–10.0s）
- **[手稿]** 视角不动；纯色背景；上方标题 *想怎么看 就怎么排*；下方软件页面
  **单屏换双屏换四分换1+5** [图片形式]
- **[补齐]** 背景 **`sc-white`**；下方软件窗口（深灰，横向 x 250–1632、下沿裁切，照参考片构图）；
  布局依次 `1x1 → 1x2 → 2x2 → corner`，每档 420ms 重排（格子真移动），末档停 400ms
- 素材位：`[素材: 6 路直播画面]`

### 【约 10–12s】弹幕

**06 · 弹幕跟着画面走**（10.0–12.0s）
- **[手稿]** 视角不动；同纯色背景；右侧标题 *弹幕跟着画面走*；左侧软件页面 **1+1+弹幕**；字体飞入后视角聚焦弹幕栏
- **[补齐]** 布局 `dm_main2`；标题从右 `translateX(40px)` 飞入；随后镜头 `scale 1.16` 聚焦弹幕栏并轻微平移；
  弹幕行 `linear` 连续上滚（**全片唯一持续运动**）
- **[用户 2026-09-22]** 软件页面**左移**（`x 250 → -250`，左侧 250px 被画面边缘遮掉），
  正好让出右侧给飞入的标题；**弹幕这一屏也换成真实截图**（由用户手动截取），
  我方只负责拼接与上滚动效 —— 到位前保留 `[素材: 弹幕画面]` 占位。
  标题留在**右上**（`top 60`）而不是原来的 `top 250`：那个位置和窗口重叠 372px，
  标题在 DOM 里先于窗口、会被整个盖住，成片里只剩右半截「着画面走」
- 素材位：`[素材: 1 路主画面 + 1 路小画面 + 弹幕文本]`

### 【约 12–14s】竖屏

**07 · 拖着竖的，他自己跟上**（12.0–14.0s）
- **[手稿]** 视角不动；同纯色背景；上方标题 *拖着竖的，他自己跟上* + *动态切换成竖屏，让你游戏之余，也能监控*；
  右侧飞入竖屏画面 **1+2 布局**；左侧是 06 的画面
- **[用户 2026-09-22 改]** 排版照他给的截图：**左侧横屏窗口落在左下、右侧竖屏窗口更高、
  两者底边对齐** —— 横屏 `scale .71` → box 约 `0,518,981x562`（左缘贴画面左边、下沿落 1080 贴底）；
  竖屏 `1243,300,430x780`（底边同样 1080，与横屏齐平；左缘距横屏右缘 262px）。
  **标题移到左上**（`left 110 / text-align left`）；副标「动态切换**成**竖屏」去掉"成"字
- **[补齐]** 左侧保留 06 定格；右侧竖屏窗口从画面外**右侧**滑入，比例 `16:9 → 9:16` 连续变形
  （`700x480 → 430x780`，1.04s），内部按 `portrait_main2`（主画面 16:9 整宽 + 2 小）重排
- 素材位：`[素材: 竖屏 1 主 + 2 小]`

### 【约 14–19s】悬停预览

**08 · 悬停预览 如此轻松**（14.0–19.0s）
- **[手稿]** 视角不动；背景未定；多张卡片**立体排列**，**向上弹出 1 张**；模拟光标移到卡片上；
  播放直播预览；**重复 3 轮**；左上标题[立体效果] *悬停预览* + *如此轻松*
- **[补齐]** 背景中灰；卡片栈 `rotateX(12deg)` 斜置，抽出那张 `translateZ(+120px)` 放大 1.06；
  每轮 1.5s：抽出 320ms → 光标移动 700ms → 封面淡入 260ms → 收回 300ms；三轮换三张卡
- 素材位：`[素材: 3 组封面 + 3 段直播预览]`

### 【约 19–29s】声道

**09 · 听完你的听你的**（19.0–24.0s）
- **[手稿]** 视角不动；**直播声音出来时音乐停止**；同 05 纯色背景；上方标题 *听完你的听你的* + *各路独立左右声道路由*；
  下方软件 **9 分布局**；模拟光标**依次打开上方三个格子的声音**
- **[补齐]** 背景 `sc-white`；布局 `3x3`；光标依次移到左上三格的音量键（各 900ms），
  点击瞬间该格控制条亮 `ACCENT` 并出现音量条；**此处 BGM 600ms 淡出、直播声进**
- 素材位：`[素材: 9 路直播画面 + 3 路原声]`

**10 · 左右声道独立**（24.0–29.0s）
- **[手稿]** 视角不动；同背景；09 标题放大到画面中心 + 软件下移 → *声音太杂乱听不清* + *怎么办*；
  然后软件上移，布局**单画面一分为二左右**，标题上移 → *左右声道独立* + *我全部都要*；
  模拟光标将左侧画面打开左声道、右侧右声道
- **[补齐]** 三段落：①标题 `scale 1→1.9` 移到中心 + 软件 `translateY(+90px)`（600ms）；
  ②软件 `translateY(-40px)` + 布局 `1x1→1x2`（480ms）+ 标题上移；③光标点左格「只播左声道」、右格「只播右声道」（各 1100ms）；
  声波左先起、右 500ms 后应答
- 素材位：`[素材: 2 路直播画面 + 左/右声道原声]`

### 【约 29–36.5s】三连问句

**11 · 为啥不用浏览器**（29.0–31.5s）
- **[手稿]** 视角不动；**另一纯色背景**；中心标题 *为啥不用浏览器*
- **[补齐]** 背景 **`sc-deepblue`**（照参考片 29.58–36.00 的 `#090c6d`）；标题 `scale .96 + opacity 0` 落定 480ms
- 素材位：无

**12 · 超快的缓冲速度**（31.5–34.0s）
- **[手稿]** 同背景；中心标题 *九路直播同开* + *超快的缓冲速度* + *平均时长约 xxx*
- **[补齐]** 「九路直播同开」留在原位接续，下方换新行；**`[数值: 平均缓冲时长]`**
- 素材位：`[素材: 缓冲速度图示]`（可选）

**13 · 超低的内存占用**（34.0–36.5s）
- **[手稿]** 同背景；中心标题 *九路直播同开* + *超低的内存占用* + *平均占用约 xxx*
- **[补齐]** 同 12 结构；**`[数值: 平均内存占用]`**；背景可切 `sc-white-warm` 与 12 区分
- 素材位：`[素材: 内存占用图示]`（可选）

### 【约 36.5–47.5s】开源 / 反馈 / 免责

**14 · 开源，拓展**（36.5–40.0s）
- **[手稿]** 视角不动；同 05 纯色背景；左侧标题 *开源，拓展*；右侧展示**软件插件接口画面**
- **[补齐]** 背景 `sc-white`；右侧"插件目录"面板（`plugins_user/名字/plugin.py`，用 `CONTENT`/`ELEVATED` 卡片逐行展开）
- 素材位：`[素材: 插件接口截图 —— 或我用 HTML 按 theme 画]`

**15 · 反馈问题**（40.0–43.5s）
- **[手稿]** 视角不动；同背景；左侧标记 *反馈问题* + *评论区，私信，github三路同行* + *牛马大学生在线修bug*
- **[补齐]** 三行左对齐逐行 `translateY(16px)→0` 错开 80ms；「三路同行」用三个并排 chip
- 素材位：`[素材: GitHub 图标]`（可选）

**16 · 免责声明**（43.5–47.5s）
- **[手稿]** 视角不动；同背景；上方标题 *视频内软件为演示效果，请以实际为准* +
  *本项目是DD监控室的二次开发版本，是个人维护的第三方工具* + *原作者：执明神君，@神君Channel*
- **[补齐]** 三行居中，主句 `TEXT1`、其余 `TEXT3`，逐行落定
- 素材位：无

### 【约 47.5–55.5s】画面墙 + 收尾

**17 · 仰视拼接成画面墙**（47.5–52.0s）
- **[手稿]** 视角**向上仰视**；同纯色背景；16 标题消失；软件 **9 分布局**；画面上移；**拼接成画面墙效果**
- **[补齐]** 相机 `rotateX(0) → rotateX(-14deg)` + `translateY(+120px)`，九分墙向上排布并把缝隙 `6px→0` 拼合，1400ms，末段停住
- 素材位：`[素材: 9 路直播画面]`

**18 · 结束**（52.0–55.5s）
- **[手稿]** 展示标题；结束
- **[补齐]** 收黑到 `sc-black` 定格（照参考片 60.00–63.37 的 3.37s 纯黑）→ 官方 logo 拼装
  （`assets/logo.svg`：三层窗格描边 → DD 扫出 → CE 亮 → 青紫粉缝合线）→ 定格 + 字标「DD监控室CE」；**不上仓库地址**
- 素材位：`assets/logo.svg`（已有）

---

## 八、素材清单（待你提供）

| # | 素材 | 用在哪 | 规格 | 状态 |
| --- | --- | --- | --- | --- |
| M1 | 直播画面 ×6 | 02 / 05 / 06 / 07 | 6–15s，16:9，可循环 | ✅ 录屏已含（见 8.1，可裁单格） |
| M2 | 直播画面 ×9 | 09 / 17 | 同上（可与 M1 复用） | ✅ 录屏已含（见 8.1） |
| M3 | **弹幕画面（截图）** | 06 | 用户**手动截取**真实弹幕画面；我方只做拼接 + 上滚动效 | **用户截取中（2026-09-22 确认）** |
| M4 | 关注卡片封面 ×3 + 直播预览 ×3 | 08 | 16:9 封面 | **待提供** |
| M5 | 直播原声 | 09 / 10 | 3 路 + 左/右各 1 | **待提供** |
| M6 | 缓冲时长 / 内存占用数值 | 12 / 13 | 两个真实数字 | **待提供** |
| M7 | 插件接口画面 | 14 | 截图或授权我重绘 | 待定 |
| M8 | 参考链接 | 03 | 你手稿写了"参考 xxx" | **待提供** |
| M9 | BGM + 音效 | 全片 | 已有 | 已有 |
| M10 | 官方 logo | 18 | 已有 `assets/logo.svg` | 已有 |

### 8.1 录屏素材（用户 2026-09-22 23:08 提供，都在 `Video_reference/`）

五段都是 **3840×2160 / 60fps**、整块软件窗口的录屏（含侧栏），可以直接缩放当窗口内容用，
不必再裁。**动效仍归我方**：录屏里已有的操作照播，标题 / 镜头 / 节奏由 composition 叠加。

| 文件 | 时长 | 抽帧看到的内容 | 对应拍 | 状态 |
| --- | --- | --- | --- | --- |
| `布局切换.mp4` | 30.48s | 1x1 → 1x2 → 2x2 → 1+5 逐档真切换 | 05 | ✅ 可用 |
| `9格布局.mp4` | 10.53s | 9 分布局（3×3 九路） | 09 | ✅ 可用 |
| `切换竖屏.mp4` | 22.72s | 多格 → 竖屏窗口出现 → 单画面；**开头有一段黑场** | 07 | ⚠️ 要定位竖屏段 |
| `左声道.mp4` | 13.80s | 1x1 单画面（DD 监控室，左侧带弹幕） | 10 | ✅ 可用 |
| `右声道.mp4` | 13.93s | 抽帧看到的是**浏览器窗口**（标签栏 + 地址栏 + 网页播放器），不是 DD 监控室 | 10 | ❌ 需重录 |

**仍然缺的**：
- 06 的**弹幕画面**（用户手动截取中，见 M3）
- 08 的**关注卡片封面 ×3 + 直播预览 ×3**（M4）—— 手稿要「多张卡片立体排列、向上弹出 1 张、重复 3 轮」
- 14 的**插件接口画面**（M7）
- 12 / 13 的**两个真实数值**（M6）

---

## 九、待你确认

1. **03 的"参考 xxx"** 是什么？（手稿没写完）
2. **12 / 13 的两个数值** —— 需要真实数字，我不编。
3. **14 的插件接口**：给截图，还是我按 `theme.py` 用 HTML 画？
4. **总长**：11–18 你留白了，我补成 **59.5s**（A 组按要求放慢到 12s）；要压到多少你说。
5. **场景色场**：我按参考片定了"白底为主 + 11 用深蓝 + 18 黑"。**要不要保留你没提的紫段？**
6. ~~**01/02 的"背景未定"**~~ → 已定：`assets/real/bg-hall.jpg`（银河 + 山脉 + 湖面，取自 `Video_reference/background.jpg`）。

---

## 十、渲染陷阱（实测踩过的坑，写新镜头前必读）

这些坑的共同点是：DOM 的 `getBoundingClientRect()` 完全正常，但**绘制**是错的。
凡涉及 3D 变换，必须用 `tools/probe-variants.mjs` 截图后数像素来验证，只看几何会被骗。

| 症状 | 根因 | 正确做法 |
| --- | --- | --- |
| 软件窗口只画出上半部分，下半部分连窗口底色都没有，直接透出背景 | 祖先层有 `transform-style: preserve-3d`，子层再做 `rotationX` / `z` 变换时 Chrome 把该子层的绘制裁到局部 y≈363（实测只剩 417/548 行） | 祖先层**不要** `preserve-3d`；立体感改成给每个 3D 元素自带 `transformPerspective`（GSAP 属性），效果等价 |
| 同上 | 参与 3D 的层写了 `will-change: transform` —— Chrome 按布局尺寸给合成层光栅化，投影超出该边界就被整块裁掉 | 这些层不要写 `will-change` |
| 拆解"飞出画面"的模块看起来在原地消失 | 父层 `overflow: hidden` 把飞出动画整块剪掉了 | 承载"飞出去"的父层不要裁切，圆角交给子元素自己 `border-radius` |
| 布局动画一顿一顿 | 补间了 `left` / `top`，布局属性会吸附到整数设备像素 | 位移用 `x` / `y`；宽高可以保留 CSS 补间（离线逐帧渲染不在乎 layout 成本，而 transform 缩放会把格子里的文字/控制条一起拉变形） |
| 时间轴上的 callback 不触发 | 渲染器用 `seek(t, suppressEvents=true)` 跳帧取画 | 所有状态变化都必须写成时间轴上的补间，不能用 `tl.call()` |
| 相邻 tween 抢同一个 transform 分量，中途跳变 | 两个 tween 都补 `x` 且开始时间错开 | 让它们同时开始、各补不同属性（一个补 `opacity`，一个补 `width/height/x/y`） |

**验证工具**（都在 `tools/`）：

- `probe-variants.mjs` + 一份 JSON 配置 → 在不同时刻各截一张图，并打印选择器的真实 `opacity` 与 `getBoundingClientRect`
- `probe-shot.ps1` → 在截图上数某一列的暗行数、找"暗→亮"的裁切边界。**模型看不到图，构图只能靠这个数出来**
- `probe-dom.mjs` → 静态服务器 + CDP，把时间轴 seek 到指定时刻后读真实几何（注意 Windows 上路径分隔符要统一成 `/`，否则资源全 404，几何全是未变换的假象）
- `.ps1` 一律**纯 ASCII 注释**：Windows PowerShell 5.1 按 ANSI 读脚本，UTF-8 中文注释会变乱码，可能破坏字符串字面量
