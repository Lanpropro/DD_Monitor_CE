# 交接：竖屏适配（DDM-PORTRAIT-2026-09-17）

本文件是一份**任务交接材料**，不是规则、也不构成授权。接手的对话应当把它当待核对的上下文，
以当时的用户要求、现行规则和核实后的文件状态为准。

- 交接编号：**DDM-PORTRAIT-2026-09-17**
- 源任务：DD 监控室 CE 竖屏适配（本次对话）
- 工作目录：`F:\CodexAppManager\Code\DD_Monitor_CE`
- 材料状态：**材料已核对，等待接手核验**（接手编号未返回，留空）
- 更新时间：2026-09-17

## 一、任务与版本

**目标**：给 DD 监控室 CE 加竖屏适配 —— 窗口变成竖的时候，排布、布局预设、
关注列表都要跟着变，而且不影响横屏。

**完成标准**（用户已确认的形态）：
1. 竖屏时侧栏变成**顶部横栏**：收起只剩一排头像（账号头像在最前，后跟关注的主播，
   同尺寸同基线）；点头像开账号菜单。
2. 展开后**不保留头像排**，换成**横向卡片条**（关注列表横着排、竖向滚轮换算成横向滚）。
3. 竖屏布局是**单独预设**（不做自动镜像）：主画面按 16:9 占满宽度，小画面填下面的空间。
   竖屏的「自动」用 `portrait_main6`（主画面 + 6 小）。
4. 横屏的一切行为保持不变。
5. 切布局**不改窗口方向**（窗口形状由用户拖，摆放跟着窗口走）。

**代码位置**：分支 `main`，本地与 `origin/main` 同步。
**以 `git log --oneline -1` 为准取当时的 HEAD**，不要相信本文件里写死的哈希 ——
写死过一次，立刻被后续的文档提交推翻了。本材料写入时 HEAD 是 `8740141`，
在此之前的功能提交到 `25f4507`（竖屏自检第 14 组）为止。
工作区在本文件写入后只剩两个未跟踪草稿：`dev/mock_portrait.py`、`dev/portrait_mock.html`。

## 二、权威资料

| 路径 | 用来认定什么 |
| --- | --- |
| `ddm/layouts.py` | 布局预设表。`PORTRAIT_LAYOUTS` / `PORTRAIT_DANMAKU_LAYOUTS`、`is_portrait_layout()`、`PORTRAIT_AUTO`、`GROUPS`（三组：普通/弹幕/竖屏） |
| `ddm/app.py` | `_build_arrangement()`（按方向重摆侧栏与画面墙）、`_apply_orientation()`、`_layout_fits()`、`_demote/_promote_native_windows()`、`_adopt_stray_tiles()`、`_on_layout_changed()` |
| `ddm/widgets.py` | `Sidebar.set_side/_sync_top_mode/_apply_scroll_axis`、`RoomStrip`（头排头像）、`RoomListBox.horizontal`（横向卡片条）、`WallGrid._relayout_portrait()` |
| `dev/selfcheck_portrait.py` | 竖屏自检 14 组 —— **改竖屏代码前先跑它** |
| `docs/portrait-mock.png`、`docs/portrait-current.png` | 已确认的形态原型图 |
| `idea.txt` | 暂缓项（含「布局变大自动填充」的登记） |
| `AGENTS.md` | 项目要求：每次改动必须提交 + 必须补/更新测试并跑通 |

## 三、决定与限制

**用户已确认**：
- 竖屏用**单独布局预设**，不做「行列自动镜像」（用户明确否掉镜像路线）。
- 展开态**不保留头像排**，改成横向卡片条。
- 头排**点头像开账号菜单**（先按这个做；点整条展开横栏的做法已被否）。
- 切布局不改窗口方向。
- 竖屏收起态只留头像排 + 一个展开/收起按钮。

**Agent 自行选择（可改，但要知道原因）**：
- 竖屏的主画面**不给高度上限**（`main_height = max(80, int(width*9/16))`）：
  主画面是主角，先满足它的 16:9，小画面去适应剩下的高度。
- 竖屏展开态**不显示**「关注中 / 排序 / 刷新」那一行，入口收进「⋯」；标题只留「多选」。
  横栏高度很宝贵（原来 447px 把墙挤矮、主画面被压成 2.2:1，现在 362px）。
- 展开/收起按钮从标题行**摘出来常驻横栏右上角**，用 `#BarToggle` 紧凑样式
  （`#SidebarToggle` 的 24px 最小尺寸会把横栏撑高）。

**未决 / 待确认**：
- 顶部横栏右侧要划一块放**账号头像 + 布局按钮 + ⋯**（用户要「和横屏一致」）——
  尚未实现，是下一步的首选动作。
- 竖屏横栏还有没有更好的高度分配（现在卡片条 130px + 按钮行）。

**已否决的路线（不要重试，除非条件变了）**：
- `:stereo-mode`、`--audio-filter=remap` 的 `aout-remap-*` 写法：用 WASAPI 环回录音
  量过，左右声道内容完全没变。声道改走进程内 PCM 回调（`ddm/audio_output.py`，
  由用户/Codex 完成，已合并进 `ddm/player.py`）。
- 「两套预设排布 + QStackedWidget 切换」：Qt 的 `addWidget` 会把控件从旧布局移走，
  两套都挂同一个控件只会留在最后一套。现在是切方向时重排（`_build_arrangement`）。

## 四、进展与运行状态

**已完成（有验证）**：
- 竖屏布局预设 6 个 + 竖屏布局菜单第三组 + 竖屏的自动用 `portrait_main6`。
- `WallGrid` 的竖屏摆放：把自己从网格里摘出来、主画面整宽 16:9、小画面自动网格。
  实测 1080×1920：主画面 1048×589（1.779）、六小 520×334。
- 侧栏 `set_side('top')` 顶部横栏：头像排（账号 + 关注，同尺寸同基线、开播粉点、置顶角标、
  放不下显示 `+N`）、展开/收起按钮常驻、`⋯` 收其余入口。
- 展开态横向卡片条：206×128 卡片向右排、竖向滚轮横向滚、拖动让位（横排用 x 算落点）。
- 头排头像**可拖到画面墙**（拖到空格子填进去、拖回原格交换且不重复）—— 自检 11b 覆盖。
- 横屏↔竖屏往返：方向/布局/头排/控件条 x 坐标都在正确状态；回横屏后左栏 248×900、列表竖排。
- 原生窗口处理：换排布前降级 + 隐藏，换完提回并兜底收回漏成顶层窗口的格子
  （顶层窗口数 4 → 1，只剩主窗口）。

**进行中 / 未完成**：
1. **顶部横栏右侧按钮/头像区**（用户明确要求，未实现）。
2. **横屏→竖屏仍剩 1 个悬浮窗**（已从 4 个降到 1 个，归属未定位）。
3. **竖屏→横屏后左栏「只剩一个关注卡片」**（用户复测仍有；Agent 用真实配置 28 个关注
   未能复现）。
4. `selfcheck_offline_badges` 失败（**改动前就红**）+ 会崩溃退出 `0xC0000409`。
5. 「布局变大自动填充」——**用户要求暂缓**，见 `idea.txt`。

**未验证**：
- 竖屏真机验收：头排点击切画面、账号菜单贴头像弹出、拖头像换画布，都只在离屏自检里验过，
  没有真鼠标验收记录。
- 悬浮栏按钮的"蓝色覆盖"问题：Agent 未能复现（离屏渲染下遮罩包围盒 144×26、
  `遮罩含整块=True`）；用户最后一次反馈是「现在看正常了」。

**运行状态**：
- 客户端由 `dev/restart-client.ps1` 启动（PID 会变；最后一次是 61496）。
  该脚本重启前打印「没有正在运行的实例」——说明**当时的客户端已经退出**，值得留意。
- 全套自检：`dev/selfcheck_*.py` 共 22 个，**21 通过**，只剩 `selfcheck_offline_badges`。
- 跑自检的环境变量：`DDM_NO_SAVE=1`、`PYTHON_VLC_LIB_PATH=<repo>\libvlc.dll`、
  `PYTHONIOENCODING=utf-8`；解释器 `F:\CodexAppManager\Code\DD_Monitor-venv\Scripts\python.exe`
  （仓库内没有 `.venv`）。
- 无本会话专属的后台任务需要移交。

## 五、接续动作

**第一步（用户已确认的范围，可直接做）**：
把顶部横栏右侧划出一块，放**账号头像 + 布局预设 + `⋯`**，和横屏一致；
把现在堆在横栏下方的那一行（布局预设 / ⋯ / 导入关注 / + 添加直播间）挪上去，
让横栏更矮。验收方法：
- `dev/selfcheck_portrait.py` 仍全过（14 组），
- 新增断言：展开态下账号头像与按钮在**同一行**、横栏高度比现在（362px）更矮，
- 手动/离屏截图确认横栏右侧那一块和横屏的账号行观感一致。

**第二步**：查剩下的 1 个悬浮窗归属（它是在 `_build_arrangement` 之后才被提成顶层窗口的？
用 `QApplication.topLevelWidgets()` 在切方向前后各打一次）。

**第三步**：查「竖屏→横屏后左栏只剩一个卡片」，需要用户提供当时的窗口尺寸或截图；
复现不了就先记录，不要盲改。

**边界**：不要顺手改「布局变大自动填充」（用户已要求暂缓）；不要动声道/音频回调
（那是另一条线的成果）；`selfcheck_offline_badges` 的失败要先确认产品判断
（取流失败的格子算不算在播），不要直接改断言。

**验收方法**：每个改动都要 ①`dev\run-checks` 那套自检全过（只剩已知的
`offline_badges`）②补/改对应自检 ③一个 Git commit（`AGENTS.md` 的要求）。
