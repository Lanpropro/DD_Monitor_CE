# 交接：竖屏适配（DDM-PORTRAIT-2026-09-17）

本文件是一份**任务交接材料**，不是规则、也不构成授权。接手的对话应当把它当待核对的上下文，
以当时的用户要求、现行规则和核实后的文件状态为准。

- 交接编号：**DDM-PORTRAIT-2026-09-17**
- 源任务：DD 监控室 CE 竖屏适配（本次对话）
- 工作目录：`F:\CodexAppManager\Code\DD_Monitor_CE`
- 材料状态：**已接手，第一步已交付**（接手是用户直接让本对话继续，未走新建任务；
  接手编号未提供，留空。接手方补记见文末「交接登记」）
- 更新时间：2026-09-18（补记；原始材料写于 2026-09-17）

## 一、任务与版本

**目标**：给 DD 监控室 CE 加竖屏适配 —— 窗口变成竖的时候，排布、布局预设、
关注列表都要跟着变，而且不影响横屏。

**完成标准**（用户已确认的形态）：
1. 竖屏时侧栏变成**顶部横栏**：收起只剩一排头像（账号头像在最前，后跟关注的主播，
   同尺寸同基线）；点头像开账号菜单。
2. 展开后**不保留头像排**，换成**横向卡片条**（关注列表横着排、竖向滚轮换算成横向滚）。
3. 竖屏布局是**单独预设**（不做自动镜像）：主画面按 16:9 占满宽度，小画面填下面的空间。
   竖屏的「自动」用 `portrait_main6`（主画面 + 6 小）。
4. 横屏的一切行为保持不变。
5. 切布局**不改窗口方向**（窗口形状由用户拖，摆放跟着窗口走）。

**代码位置**：分支 `main`。**以 `git log --oneline -1` 为准取当时的 HEAD**，
不要相信本文件里写死的哈希 —— 写死过一次，立刻被后续的文档提交推翻了。
本材料写入时 HEAD 是 `8740141`；接手方补记时（2026-09-18）HEAD 是先落的
`2a67f40`（第一步交付），它的前 6 个提交 `a3c6391`→`e958a73` 是源任务在材料
写完之后继续做的（见第四节）。本地领先 `origin/main` 8 个提交，**没有推送**。
工作区只剩两个未跟踪草稿：`dev/mock_portrait.py`、`dev/portrait_mock.html`
（源任务留下，接手方没有触碰）。

## 二、权威资料

| 路径 | 用来认定什么 |
| --- | --- |
| `ddm/layouts.py` | 布局预设表。`PORTRAIT_LAYOUTS` / `PORTRAIT_DANMAKU_LAYOUTS`、`is_portrait_layout()`、`PORTRAIT_AUTO`、`GROUPS`（三组：普通/弹幕/竖屏） |
| `ddm/app.py` | `_build_arrangement()`（切方向：只改根布局方向 + 伸缩，不重挂父容器）、`_apply_orientation()`、`_layout_fits()`、`_on_layout_changed()`。注意 `_demote/_promote_native_windows()`、`_adopt_stray_tiles()`、`_suspend_players_for_arrangement()` 已被 `e958a73` 删除，别在旧资料里找它们 |
| `ddm/widgets.py` | `Sidebar.set_side/_sync_top_mode/_apply_scroll_axis`、横栏那一行的 `_adopt_bar_row/_release_bar_row/_account_row_should_show`、`RoomStrip`（头排头像）、`RoomListBox.horizontal`（横向卡片条）、`WallGrid._relayout_portrait()` |
| `dev/selfcheck_portrait.py` | 竖屏自检 14 组（+ 接手方加的 12b 组）—— **改竖屏代码前先跑它** |
| `dev/selfcheck_portrait_native.py`、`dev/selfcheck_portrait_sidebar_return.py` | 源任务后来补的两条竖屏回归：跨屏切方向不换视频 HWND / 不产生悬浮格子；竖屏回横屏后关注卡片仍逐行排列 |
| `dev/selfcheck_layout_none.py` | `wall_rooms=None` 时布局变大不能把关注列表自动填上墙 |
| `dev/preview_bar.py` → `docs/portrait-bar.png` | 顶部横栏的展开态/收起态截图（接手方第一步的形态证据） |
| `docs/portrait-mock.png`、`docs/portrait-current.png` | 早先的形态原型图（`portrait-current.png` 是横屏左栏，不是竖屏现状） |
| `idea.txt` | 暂缓项（含「布局变大自动填充」的登记） |
| `PROJECT_STATE.md` | 另一条线（接手审查任务 DDMCE-20260917-PORTRAIT-CONTINUE-01）的状态载体，**不归本材料管，也不由接手方改写** |
| `AGENTS.md` | 项目要求：每次改动必须提交 + 必须补/更新测试并跑通 |

## 三、决定与限制

**用户已确认**：
- 竖屏用**单独布局预设**，不做「行列自动镜像」（用户明确否掉镜像路线）。
- 展开态**不保留头像排**，改成横向卡片条。
- 头排**点头像开账号菜单**（先按这个做；点整条展开横栏的做法已被否）。
- 切布局不改窗口方向。
- 竖屏收起态只留头像排 + 一个展开/收起按钮。

**Agent 自行选择（可改，但要知道原因）**：
- 竖屏的主画面**不给高度上限**（`main_height = max(80, int(width*9/16))`）：
  主画面是主角，先满足它的 16:9，小画面去适应剩下的高度。
- 竖屏展开态**不显示**「关注中 / 排序 / 刷新」那一行，入口改成搜索框旁边的小图标。
  横栏高度很宝贵（最早 447px 把墙挤矮、主画面被压成 2.2:1，当时的 362px 现在压到 196px）。
- 展开/收起按钮从标题行**摘出来常驻横栏右上角**，用 `#BarToggle` 紧凑样式
  （`#SidebarToggle` 的 24px 最小尺寸会把横栏撑高）。
- 横栏怎么排（接手方按用户实机截图定下来的形态）：**第一行**搜索框（收起时换成
  头像排）+「多选 ✓ / 排序 ↑↓ / 刷新」三个方形小图标（`#BarIcon`）+ 展开键；
  **第二行**横向卡片条 + 右侧单独一块（`Sidebar._bar_right`），那块里
  **账号条 / 布局预设 / 设置**竖排。收起时右侧那一块整块收掉、账号头像回到
  第一行最右端并缩成 36x36 小圆点。账号条展开时留昵称、给足宽度（上限 220px）。
  「⋯」按钮已按用户要求去掉：导入关注 / 添加直播间 进账号菜单。
- 竖屏切方向**不再重挂父容器**（`e958a73`）：根布局只改排列方向 + 伸缩，
  侧栏和画面墙始终挂在 root 下，VLC 的原生 HWND 就不会被 Qt 销毁。

**未决 / 待确认**：
→ **所有待办、未决、暂缓项统一收在第七节「待办与未决清单」**，这里不再重复，
免得两处不同步。

**已否决的路线（不要重试，除非条件变了）**：
- `:stereo-mode`、`--audio-filter=remap` 的 `aout-remap-*` 写法：用 WASAPI 环回录音
  量过，左右声道内容完全没变。声道改走进程内 PCM 回调（`ddm/audio_output.py`，
  由用户/Codex 完成，已合并进 `ddm/player.py`）。
- 「两套预设排布 + QStackedWidget 切换」：Qt 的 `addWidget` 会把控件从旧布局移走，
  两套都挂同一个控件只会留在最后一套。现在是切方向时重排（`_build_arrangement`）。
- 「切方向时降级原生窗口 → 重挂父容器 → 提回原生窗口 + 兜底收悬浮窗」
  （`_demote/_promote_native_windows`、`_adopt_stray_tiles`、切方向前停播再重启）：
  这一整套在 `1c0f399`/`e958a73` 里被删掉了。它是在治标 —— 换父容器本身就会
  销毁 VLC 绑定的 HWND，先后出现过黑屏、叠层显隐被冲掉、同一格两个 resolver
  抢播放器。正解是**不换父容器**，只改根布局方向。别再把那套机制加回来。

## 四、进展与运行状态

**已完成（有验证）**：
- 竖屏布局预设 6 个 + 竖屏布局菜单第三组 + 竖屏的自动用 `portrait_main6`。
- `WallGrid` 的竖屏摆放：把自己从网格里摘出来、主画面整宽 16:9、小画面自动网格。
  实测 1080×1920：主画面 1048×589（1.779）、六小 520×334。
- 侧栏 `set_side('top')` 顶部横栏：头像排（关注主播，同尺寸同基线、开播粉点、置顶角标、
  放不下横向滚）、账号头像 + 展开/收起按钮常驻右端。
- 展开态横向卡片条：206×128 卡片向右排、竖向滚轮横向滚、拖动让位（横排用 x 算落点）。
- 头排头像**可拖到画面墙**（拖到空格子填进去、拖回原格交换且不重复）—— 自检 11b 覆盖。
- 横屏↔竖屏往返：方向/布局/头排/控件条 x 坐标都在正确状态；回横屏后左栏 248×900、列表竖排。
- 原生窗口处理：~~换排布前降级 + 隐藏，换完提回并兜底收回漏成顶层窗口的格子~~
  —— 这套已由 `e958a73` 整体删除，改成切方向不换父容器（见第三节「已否决的路线」）。
- **顶部横栏右侧那一块**（`2a67f40`，接手方第一步）：搜索框和
  「账号条 / 布局预设 / 展开键」并成**同一行**；导入关注、添加直播间收进账号菜单。
  横栏高度展开 **362 → 196px**、收起 **76 → 52px**
  （196 = 横栏一行 36 + 间距 6 + 卡片条 130 + 横向滚动条 8 + 上下边距 16）。
  自检第 12b 组钉住「图标/账号头像/布局预设/设置并排同一行、多选搬下来、⋯ 已去掉」；
  截图 `dev/preview_bar.py` → `docs/portrait-bar.png`。
- **收起态头像排**（`20732ad`，接手方，按用户实测的三条报错改）：
  全部关注都摆出来（原来硬编码最多 9 个）、用列表里已下载的真实头像
  （原来一直是字母占位）、账号头像从最前面挪到最右端和展开键并排。
  放不下时整排横向滚（复用 `CarouselScroll`），收起态不显示滚动条、横栏仍 52px。
- **展开态两行**（接手方，按用户对着实机截图的要求改）：第一行是搜索框 +
  「多选 ✓ / 排序 ↑↓ / 刷新」三个小图标（`#BarIcon` 样式，用最普通的字形 ——
  雅黑会把 ⇅ ☑ 这类组合符号掉成怪字形）+ 展开键；第二行是横向卡片条 +
  **右侧单独一块**，那块里「账号 / 布局预设 / 设置」竖排（`_bar_right`）。
  「⋯」按钮已去掉（`RowMoreButton` 类和 `_more_actions()` 一起删）：导入关注 /
  添加直播间进账号菜单。横栏 224 → **192px**。
- **收起态账号按钮**（接手方，用户报「不需要那么大的按钮」）：收起时账号条
  宽度按头像算（36x36 小圆点），不再沿用昵称宽度变成一条长胶囊。
- **A1–A4 四条微调 + 后续修正**（接手方 2026-09-18 一轮做完，用户对着实机截图提的）：
  - A1 竖屏：标题行搬进横栏第一行当 **logo**（只露「DD 监控室」，副标题藏起来），
    搜索框因此缩短（1080 宽下 928 → 692px），右侧三个图标照旧贴右端。
  - A2 竖屏：收起态头像的置顶角标从 14x14 方角 QLabel 换成
    **沿圆周的一段弧**（`PinnedArc`，90°..180°、3px、圆头），不再戳到圆外面。
  - A3 竖屏：卡片条右边那一块**只占卡片条那一行**（不侵占第一行）。三个按钮都用
    **横屏那套高度**（账号 34、布局预设/设置 36）、**左右长度相同**（都撑满这一块，
    1080 下 126px），多出来的高度均分给两条**空白**（各 11px，里面那条 1px 分割线
    居中：上 5 下 5）；空白高度由 `_sync_bar_gaps()` 按**缩略图高度**
    （`NAV_ITEM_HEIGHT`，不是整条卡片行 —— 那一行还含 8px 滚动条）现算，
    所以**最下面按钮的下沿和缩略图下沿齐平**，块本身仍是 138 撑满那一行
    （末尾留一条 stretch 吃掉滚动条那 8px）。块内 stretch 每次挪账号都要
    `_apply_bar_block_stretches()` 重设。
  - A4 横屏：卡片左上角的置顶**小三角换成一段弧**（`NavItem.paintEvent`，
    半径 = `theme.RADIUS_MD`，顺着卡片圆角走）；竖屏卡片条用的是同一个类。
  - 追加修正 1：`↑↓` 原来用字形画，雅黑下墨迹比按钮中心**偏左 5px**、
    两个箭头还会重叠；改成 `BarIconButton` 自己画（勾 / 上下箭头分开画，
    和 `RefreshButton` 的手绘圆环一个路子）。用户后来指出「图案下面还藏了一个
    图案」—— 那是**排序按钮带菜单时 Qt 多画的下拉小三角**，已用
    `#BarIcon::menu-indicator { image: none; width: 0 }` 去掉（横屏的「排序」
    是 ChipButton，不受影响）。自检第 12b 组现在会量三个图标墨迹包围盒的中心。
  - 追加修正 2：**窄格子里 LIVE 浮标把人数截掉了**。根因是 `StreamBadge`
    用 `widget.fontMetrics()` 量宽度 —— QSS 的 `font-size` 要 polish 之后才落到
    控件上，构造时量出来偏小（144 vs 实际 152），画的时候字变大就被自己的遮罩
    截断。现在新增 `_badge_font()`：量宽度和画文字都用同一个字体
    （`StreamBadge` / `TimeBadge` 都换了）。另外「挤不下就收起人数」改成
    「只有格子窄到连 `LIVE + 人数` 都放不下才收起」，一般情况让控制条折到第二行。
  - 追加修正 3：**竖屏点「多选」会把横栏顶高、画布抖一下**（用户报「多出来一排」）。
    原来 `batch_bar` 是 `_layout` 里独立一行，露出来横栏就长高 36px。现在竖屏下
    把它**借到横栏第一行**（和搜索框轮流坐）：进多选态顶掉搜索框和三个图标、
    退出再换回来；第一行高度固定成标准控件那一档（36），所以两种状态横栏都是
    **196px**，画面墙上沿不动。删除/取消就是原来那两个按钮（36px、样式表那套
    圆角），**没有压矮**。`set_select_mode()` 在竖屏走 `_sync_top_mode()` 统一
    决定显隐；横屏仍是原来「多选条 ↔ 导入添加那一行」互换。自检第 12c 组钉住
    「横栏高和主画面高都不变、多选条在第一行、删除/取消 ≥ 标准高度」。
- **关注栏横向滚动条对齐主题**（`fbc8931`，接手方）：横向卡片条原来掉回系统原生
  滚动条（灰底 + 两端箭头、12px），现在和横屏那条竖向条同源同色同粗
  （`theme.SCROLLBAR_SIZE = 8`，两轴共用这个常量）；卡片底部不再被切掉。
  第 12 组加了断言：两轴规则都在、滑块同色、渲染粗细 8px、视口高度 ≥ 卡片高度。
- **从竖屏回横屏后左栏滚动失效**（用户报的；本轮修）：`set_side("left")` 从来没
  调过 `_apply_scroll_axis()`，竖屏那套滚动轴状态（`horizontal_only=True`、竖条
  `AlwaysOff`、横条 `AsNeeded`）会一路留着 —— 回横屏后竖向滚动条不再出现，
  滚轮还被 `CarouselScroll` 当成横滚吞掉，整列滚不动。现在切回左栏时复用
  已有的 `_apply_scroll_axis()` 还原；自检第 9 组加了「拖回横屏后滚轮真的能滚」
  的实测断言（撤掉修复即红）。
- **关窗释放播放器后的野指针**（`a7b51a2`，接手方，对应下面第 4 条）：取证定位到
  `444b1e4` 那版代码 —— `closeEvent` 只 `release()` 不清 `players`，关窗后投递的
  `resolved` 会在 `_play_on` 里拿到已释放的实例（日志里的 access violation）。
  现在关窗前清表 + 关窗后迟到的回调直接返回；新自检
  `dev/selfcheck_close_release.py`（撤掉修复即变红）。
- 源任务在材料写完之后继续做的 6 个提交（`a3c6391` → `e958a73`，01:02–04:19）：
  - `a3c6391`：回横屏时把横栏留下的卡片 x 清零（**交接里第 3 项**：回横屏只剩一张卡）
    + 新自检 `selfcheck_portrait_sidebar_return.py`。
  - `49193d9`：`Tile(room, self)` 先挂画面墙（竖屏手动 setGeometry 的格子不再变成
    顶层悬浮窗）；提回原生窗口改用 `isHidden()` 判断；`set_layout()` 之后再兜一次。
  - `444b1e4`：原生窗口升降级改成**保存/恢复叠层显隐**；`TilePlayer.bind()` 记住
    HWND、多余的 `set_hwnd` 不再重复下发；画质策略不再让同一格取两次流。
  - `050b045`：加 `Tile.prepare_video_surface()`（VLC 绑定前先备好原生视频面）。
  - `1c0f399`：构造阶段不碰原生窗口；切方向前完整停播、记下暂停态、排布完再重启。
  - `e958a73`：**把上面那一串升降级/停播机制全删了**，`_build_arrangement` 只改根布局
    方向 + 伸缩，不重挂父容器 —— 这才是「剩 1 个悬浮窗 / 切方向黑屏」的根因解法。

**进行中 / 未完成**：
1. ~~顶部横栏右侧按钮/头像区~~ —— 已完成（`2a67f40`），待真机看一眼观感。
2. ~~横屏→竖屏剩 1 个悬浮窗~~ —— `49193d9`+`e958a73` 之后，换父容器这条路径没了；
   `selfcheck_portrait_native.py`（跨屏切方向不换视频 HWND、不产生悬浮格子）通过。
   真机上还没复测过。
3. ~~竖屏→横屏后左栏只剩一个卡片~~ —— `a3c6391` 修掉，自检
   `selfcheck_portrait_sidebar_return.py` 通过；真机上还没复测过。
4. `selfcheck_offline_badges` 失败（**改动前就红**）+ 会崩溃退出 `0xC0000409`。
   这条自检自己的 0xC0000409 还没查；客户端那次「自行退出」已单独取证并修（`a7b51a2`）。
5. 「布局变大自动填充」——**用户要求暂缓**，见 `idea.txt`。

**未验证**：
- 竖屏真机验收：头排点击切画面、账号菜单贴头像弹出、拖头像换画布、新版横栏右侧那一块，
  都只在离屏自检/离屏截图里验过，没有真鼠标验收记录。
- 悬浮栏按钮的"蓝色覆盖"问题：Agent 未能复现（离屏渲染下遮罩包围盒 144×26、
  `遮罩含整块=True`）；用户最后一次反馈是「现在看正常了」。

**运行状态**：
- 客户端由 `dev/restart-client.ps1` 启动（PID 会变）。补记时**没有确认到客户端在运行**
  （只有本会话自己的 python 进程），要真机验收得先跑这个脚本。
- 全套自检：`dev/selfcheck_*.py` 现在 **25 个**（材料写时 22 个）：
  23 个通过，`selfcheck_offline_badges` 仍是已知红，`selfcheck_plugins` 只在
  「沙箱禁止在 mkdtemp 目录下建子目录」时失败（放宽权限后通过，与竖屏改动无关）。
- 跑自检的环境变量：`DDM_NO_SAVE=1`、`PYTHON_VLC_LIB_PATH=<repo>\libvlc.dll`、
  `PYTHONIOENCODING=utf-8`；解释器 `F:\CodexAppManager\Code\DD_Monitor-venv\Scripts\python.exe`
  （仓库内没有 `.venv`）。
- 无本会话专属的后台任务需要移交。
- 接手方跑自检时会在 `work/tmp/` 下留下 3 个受限临时目录（当前沙箱删不掉，
  会让 `git status` 带 `could not open directory` 警告；`work/` 在 `.gitignore` 里，
  不进版本库）。

## 五、接续动作

**第一步（用户已确认的范围，可直接做）**：**已完成并提交（`2a67f40`）**，
按用户后续两轮实测又改过（`20732ad` 收起态头像排 + 本轮展开态一行化）。
横栏右侧现在是**账号头像 + 布局预设 + 设置**三样并排（用户明确要求横向摆、
不要再改横栏高度），「⋯」去掉，多选/排序/刷新做成搜索框旁边的小图标。验收：
- `dev/selfcheck_portrait.py` 14 组 + 12b 全过，
- 新增 12b 组：展开态图标/账号头像/布局预设并排**同一行**、横栏比 362px 矮
  （实测 196px）、「⋯」已去掉、导入/添加在账号菜单里，
- 截图：`dev/preview_bar.py` → `docs/portrait-bar.png`（展开 + 收起两态）。
- **真机（真鼠标）观感还没看过**，这是第一步剩下的唯一尾巴。

**第二步**：查剩下的 1 个悬浮窗归属 —— 源任务已用 `49193d9`+`e958a73` 从根上解掉
（不再换父容器），`selfcheck_portrait_native.py` 覆盖；剩真机复测。

**第三步**：查「竖屏→横屏后左栏只剩一个卡片」—— 已由 `a3c6391` 修掉，
`selfcheck_portrait_sidebar_return.py` 覆盖；剩真机复测。

**下一步**：等用户给新任务（接手方按用户 2026-09-18 的指示：先读懂源任务 6 个提交，
再落实本材料的第一步；后续任务由用户指定）。可选的真机验收入口：
`dev\restart-client.ps1` 起客户端，把窗口拖成竖屏看横栏右侧那一块。

**边界**：不要顺手改「布局变大自动填充」（用户已要求暂缓）；不要动声道/音频回调
（那是另一条线的成果）；`selfcheck_offline_badges` 的失败要先确认产品判断
（取流失败的格子算不算在播），不要直接改断言。不要改写 `PROJECT_STATE.md`
（属于另一条接手审查任务）。

**验收方法**：每个改动都要 ①`dev\run-checks` 那套自检全过（只剩已知的
`offline_badges`）②补/改对应自检 ③一个 Git commit（`AGENTS.md` 的要求）。

## 六、交接登记（接手方 2026-09-18 补记）

- 交接编号：`DDM-PORTRAIT-2026-09-17`
- 源任务编号：未提供
- 工作目录：`F:\CodexAppManager\Code\DD_Monitor_CE`（仅本机路径）
- 接手方式：用户直接在**本对话**里要求继续（没有新建接手任务），接手编号留空
- 交接状态：**已接手**；第一步已提交，正在等用户给后续任务
- 并存情况：仓库里还有另一条线的状态载体 `PROJECT_STATE.md`（接手审查任务
  `DDMCE-20260917-PORTRAIT-CONTINUE-01` / `01a0affe-9021-7391-ba8c-a828e9801df4`），
  它等的正是「竖屏适配收拢」。**本材料不替它登记，也不改写它**。
- 补齐说明（接手时的并发实况）：本材料写完后，源任务又提交了 6 个 commit
  （`a3c6391`→`e958a73`），而接手方的第一步改动在**未提交状态**下和这些提交共存了
  11 小时（`ddm/widgets.py` 被双方先后改过）。接手方核验结论：两者互不覆盖，
  当前 `git diff` 只剩接手方自己的改动，已在 `2a67f40` 提交，全量自检通过。
  **教训**：同一目录里两方并行时，未提交改动不要跨小时放着，尽早 commit 或先声明归属。

## 七、待办与未决清单（2026-09-18 整合）

之前散在「未决 / 待确认」、`idea.txt` 和用户逐次实测反馈里的问题，统一收在这里。
每条都写清范围、线索位置和当前状态；**没做的一律不写进第四节「已完成」**。

### A. 用户 2026-09-18 新报（**已做完**，详情见第四节最后一条）

| # | 范围 | 结果 |
| --- | --- | --- |
| A1 | 竖屏 | 搜索框缩短 + 左侧 logo：标题行搬进横栏第一行，搜索框 928 → 824px |
| A2 | 竖屏 | 收起态置顶角标改画圆弧（`PinnedArc`），贴合圆形头像边缘 |
| A3 | 竖屏 | 账号那一块三个按钮同宽、无缝（spacing 0 + 撑满） |
| A4 | 横屏 | 卡片置顶小三角换成顺着圆角的一段弧（`NavItem.paintEvent`） |

自检第 10b/12b 组都把「真的画出来了」（数强调色像素）和「没有缝」（坐标相邻）
钉住了；形态截图仍是 `docs/portrait-bar.png`。

### B. 之前记录、仍然开着

| # | 范围 | 问题 | 线索位置 |
| --- | --- | --- | --- |
| B1 | 竖屏 | ~~「放到主画面」会切到横屏 `main4`~~ **已修 ✓**：`focus_room()` 现在按当前方向挑（竖屏→`portrait_mainN`），并改走 `set_layout()`（顺带修好 `_danmaku_cell` 没跟着换的老毛病）；`_on_fullscreen()` 也把布局记到当前方向名下 | `ddm/widgets.py:focus_room` + `ddm/app.py:_on_fullscreen`；自检第 16 组 |
| B2 | 竖屏 | 展开态账号条昵称被截成「凰Pr...」 | **用户 2026-09-18：「不用太在意」，暂不动** |
| B3 | 通用 | `selfcheck_offline_badges` 的产品判断：**取流失败的格子算不算「在播」**；另外它**不稳定**（单跑 3/3 红，连跑偶尔绿，像时序/顺序依赖） | `dev/selfcheck_offline_badges.py` + `idea.txt` 第 4 条；**用户 2026-09-18：「当我注意到我会告诉你」→ 挂着等用户** |
| B4 | 通用 | ~~「布局变大自动填充」~~ **用户 2026-09-18：「已经解决」**；自检第 14 组 + `dev/selfcheck_layout_none.py` 仍钉着「新格子保持空位」 | `idea.txt` 最后一段 |
| B5 | 验收 | ~~真机验收尾巴~~ **用户 2026-09-18：「应该不存在问题」→ 关闭** | — |
| B6 | 环境 | `work/tmp/` 下 3 个受限临时目录删不掉 | **用户 2026-09-18：「自己考虑」→ 低影响（`work/` 已 gitignore，只是 `git status` 多几行警告），不再列为待办** |

### D. 用户 2026-09-18 第二批实测 bug（**已修**）

| # | 范围 | 现象 | 修法 |
| --- | --- | --- | --- |
| D1 | 横竖屏 | 横屏 `1+2` 拖成竖屏变成 **`1+2+弹幕`**；`4 分`（`2x2`）也变成 `1+2+弹幕` | `_apply_orientation()` 原来**先信任配置里存的 `layout_portrait`**，用户以前在竖屏存过带弹幕的布局就会一直用它，对映根本没跑。改成**对映优先**（`counterpart()` 先算，配置只在对映算不出来时兜底）。自检第 18 组：横屏 `main2` + 配置里躺着 `portrait_dm2` → 竖屏必须是 `portrait_main2`；横屏 `2x2` → `portrait_main2/4`（不能是弹幕那套） |
| D2 | 菜单 | 布局预设里不同卡片**大小不一样、上下没对齐**（名字长短不一，卡片跟着宽窄不同） | `LayoutPicker._even_cards()`：所有卡片统一尺寸（宽度按最长名字算、高度按图标 + 一行文字），图标尺寸统一。自检 `selfcheck_nav_layout` 第 9 组加了「卡片尺寸只有一个值」「行间距只有『正常』和『跨小标题』两种」 |

### C. 用户 2026-09-18 的布局逻辑大改（**已做完**，详情见第四节）

| # | 范围 | 结果 |
| --- | --- | --- |
| C1 | 横竖屏 | **对映切换**：拖窗口换方向时按「能放几路」找最相似的那套（横屏 `main2` ↔ 竖屏 `portrait_main2`，弹幕对弹幕），不再一律退成「自动」——`layouts.counterpart()`，自检第 17 组 |
| C2 | 菜单 | **只列本方向的布局**：横屏不给竖屏预设（套上去画面会变形），竖屏只列竖屏那一栏——`LayoutPicker(portrait=…)`，自检第 15 组 + `selfcheck_nav_layout` 第 9 组 |
| C3 | 菜单 | 普通布局里在「主画面 + 2 小」处**换行 + 小标题「大带小」**，把平分布局和大带小分开（`"section"` 字段） |
| C4 | 菜单 | 命名规范化（`上主画面 + 下 2 小`、`主画面 + 5 小环绕`、`双主画面 + 4 小`…），并把镜像的一对挨着放：`主画面 + 2 小` 后面紧跟 `2 小 + 主画面（镜像）` |
| C5 | 菜单 | **去掉 1+6**（`main6`）；竖屏「自动」用的 `portrait_main6` 不受影响；老配置里存着 `main6` 会被安全折回「自动」 |
| C6 | 画面墙 | 「布局变大保留空位、不把留存直播间填入」——自检第 14 组 + `selfcheck_layout_none.py` 覆盖（用户说已解决，本轮复核仍通过） |
