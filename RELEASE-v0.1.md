# DD监控室CE v0.1

第一个公开发布版。多路 B 站直播间监控画面墙，横竖屏两套排布，自带弹幕与音频路由。

## 和原版有什么差别

**画面墙**
- 横屏预设：平分布局（单画面 / 上下两分 / 左右两分 / 四分 / 六分 / 九分）
  与大带小布局（主画面 + 2/3/4 小、上主画面 + 下 2 小、主画面 + 5 小环绕、
  双主画面 + 4 小、2 小 + 主画面（镜像））
- 竖屏预设：主画面按 16:9 占满宽度 + 小画面自动网格（+2 / +4 / +6 小，
  以及对应的带弹幕版本）
- 拖窗口换方向时按「能放几路」自动对映到最相似的那套（横屏 1+2 ↔ 竖屏 1+2）
- 布局菜单只列当前方向能用的布局；「大带小」单独一段，镜像布局挨着放
- 从格子右键「放到主画面」：竖屏下切竖屏预设，画面不会被压变形
- 布局变大时新格子保持空位，不会把别处的直播间自动填进来

**竖屏**
- 顶部横栏两行：logo + 搜索 + 多选/排序/刷新 + 展开键；下面一行是横向卡片条
- 卡片条右侧独立一块：账号 / 布局预设 / 设置（宽度等高、下沿与缩略图齐平）
- 点「多选」时多选条顶掉搜索那一行，横栏高度不变（画面墙不抖）
- 收起键在竖屏显示上下箭头，横屏是左右箭头

**播放与音频**
- 每格独立 libVLC 播放器，共享一个 libvlc 实例；主/次画质分级
- 每格可单独设音量，并把该格音频独立路由到左/右声道（PortAudio）
- 悬浮预览、暂停、画质切换、关掉某一路留空位

**关注列表与弹幕**
- 关注列表横向/纵向两套样式，支持置顶、排序、多选删除、搜索
- 拖头像进画面墙换画布；点顶排头像高亮选中
- 弹幕面板（blivedm）：贴底整宽、字号可调、可发送（需登录）

**其它**
- 插件体系（`plugins_user/<名字>/plugin.py`）
- 深色主题、圆角、自绘图标；窗口状态与布局按横/竖屏分别记住

### 更现代化的 UI

- 界面整个重做：从「顶部菜单 + 一堆独立窗口」换成**画面墙 + 侧栏**。关注列表卡片化（竖屏时是一条横向卡片条），每一格是圆角画面 + 悬浮控制条，空位 / 下播 / 连接失败 / 暂停 / 缓冲各有各的画面态。
- **竖屏适配**（原版没有）：顶部横栏两行 —— 第一行是 logo、搜索框、多选/排序/刷新图标、展开键；第二行是横向卡片条和右侧单独一块（账号 / 布局预设 / 设置）。主画面按 16:9 占满整行宽度，小画面自动铺满下面。
- **自绘控件与图标**：`ddm/theme.py` 把配色、字号、圆角、滚动条统一成设计令牌；勾选、排序箭头、刷新圆环、收起箭头、置顶角标全部手绘，不再用字体里那类在雅黑下会走形的符号。
- **浮层不挡画面**：LIVE 和人数、直播间标题改成**鼠标移到格子上才出现**（原版一直挂着，多了还互相压）；竖屏点「多选」时多选条顶到第一行，横栏高度不变，画面墙不跟着抖。

### 更自定义的设置

- **每一路独立**：音量、静音、画质（原画 / 蓝光 / 高清 / 低清），外加**左右声道独立路由** —— 可以把某一路单独丢到左耳或右耳（原版只能每格调音量，没有这个）。
- **布局**：13 套普通布局（平分布局 + 大带小，菜单里分段、镜像成对）+ 6 套弹幕布局 + 6 套竖屏预设；菜单**只列当前窗口方向能用的**，不会出现「选了不对方向的布局、画面被压扁」。
- **布局记忆与对映**：横屏、竖屏各记一套；把窗口拖成另一个方向时，按「能放几路」自动切到最相似的那一套（横屏 1+2 ↔ 竖屏 1+2，弹幕对弹幕）。
- **不会自作主张**：布局变大时多出来的格子**保持空白**，不会把关注列表里下一个直播间自动填进去；关掉某一路只清空那一格，位置留着。
- **关注列表**：置顶、拖动排序、三种排序（自定义 / 开播优先 / 导入顺序）、多选删除、搜索、悬停预览开关。
- **弹幕**：条数上限、字号滑块、屏蔽词、字体、粉丝牌圆角、表情包。
- **插件**：4 类扩展点（本体事件 / 平台 / 格子右键菜单 / 发弹幕），放 `plugins_user/<名字>/plugin.py`，接口说明见 [`docs/plugins.md`](docs/plugins.md)。
- **快捷键**可改：`F` 把鼠标那一路设为主画面、`Esc` 还原上一个布局、`M` 只保留那一路的声音。

### 更流畅的体验

- **共用一个 libvlc 实例**：原版是每个画面各建一个 `vlc.Instance()`，这里全局一个实例 + 每格一个 `MediaPlayer`，开十几路时内存和初始化开销都小得多。
- **播放更稳**：断流自动重连（5 秒起、逐次翻倍、最多 60 秒）、画面卡死自动刷新、下播**清黑但保留格子**、接口说回开播**自动接上**；**取流失败不等于下播**，只显示「连接失败」并继续重试。
- **操作不打断**：换布局、换方向都不会重启播放器；换方向自动对映，不用重新选布局。
- **后台加载**：头像、封面、表情都走加载线程 + 本地缓存（`cache/avatars`、`cache/covers`、`cache/emoticons`），长列表滚动不卡。
- **开箱即用**：发布页下载 `DDMonitorCE-v0.1-exe.zip`，解压后双击 exe 就能跑（免装 Python，VLC 运行库已内置）；原版要自己装 Python、VLC 和一堆依赖。

> 原版有、这一版还没有搬过来的：热榜/热播主播列表、独立悬浮窗、硬解/软解切换、全局画质与全局音频（含杜比、仅音频）、缓存大小设置、开播自动播放、自动更新与崩溃上报、macOS / Linux 打包。录制、发弹幕、接别的直播平台按设计走插件，不放进本体。

## 运行

- **exe 便携版（推荐）**：下载 `DDMonitorCE-v0.1-exe.zip`，**解压后直接双击里面的
  `DD监控室CE-v0.1-exe.exe`** 就能用（不需要装 Python）。配置 / 缓存 / 日志都在解压出来的
  那个目录下（`utils\config.json`、`cache\`、`logs\`），删掉 `utils\config.json` 等于恢复出厂。
  里面的 `_internal\`（含 `libvlc.dll` 和 `plugins\`）是运行库，别删。
  不想解压也行：本地 `results\DD监控室CE-v0.1-exe\` 就是同一个目录。
- **源码便携包**：`results\DD监控室CE-v0.1\` 整个目录拷走 →
  ① 机器上有 Python 3.12/3.13：`python -m venv .venv` →
  `.venv\Scripts\pip install -r requirements.txt` → 双击 `run.cmd`；
  ② 本机已有现成虚拟环境（`F:\CodexAppManager\Code\DD_Monitor-venv`）时
  双击 `run.cmd` 即可，它会自己找到解释器。

## 许可

程序本体见 `LICENSE`；第三方组件（libVLC、PySide6、python-vlc 等）见 `NOTICE.md`。
