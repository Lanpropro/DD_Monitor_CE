# DD监控室CE v0.1

第一个公开发布版。多路 B 站直播间监控画面墙，横竖屏两套排布，自带弹幕与音频路由。

## 和原版有什么差别

**画面墙**
- 横屏预设：平分布局（单画面 / 上下两分 / 左右两分 / 四分 / 六分 / 九分）
  与大带小布局（主画面 + 2/3/4 小、上主画面 + 下 2 小、主画面 + 5 小环绕、
  双主画面 + 4 小、2 小 + 主画面（镜像））
- 竖屏预设：主画面按 16:9 占满宽度 + 小画面自动网格（+2 / +4 / +6 小，
  以及对应的带弹幕版本）
- 拖窗口换方向时按「能放几路」自动对映到最相似的那套（横屏 1+2 ↔ 竖屏 1+2）
- 布局菜单只列当前方向能用的布局；「大带小」单独一段，镜像布局挨着放
- 从格子右键「放到主画面」：竖屏下切竖屏预设，画面不会被压变形
- 布局变大时新格子保持空位，不会把别处的直播间自动填进来

**竖屏**
- 顶部横栏两行：logo + 搜索 + 多选/排序/刷新 + 展开键；下面一行是横向卡片条
- 卡片条右侧独立一块：账号 / 布局预设 / 设置（宽度等高、下沿与缩略图齐平）
- 点「多选」时多选条顶掉搜索那一行，横栏高度不变（画面墙不抖）
- 收起键在竖屏显示上下箭头，横屏是左右箭头

**播放与音频**
- 每格独立 libVLC 播放器，共享一个 libvlc 实例；主/次画质分级
- 每格可单独设音量，并把该格音频独立路由到左/右声道（PortAudio）
- 悬浮预览、暂停、画质切换、关掉某一路留空位

**关注列表与弹幕**
- 关注列表横向/纵向两套样式，支持置顶、排序、多选删除、搜索
- 拖头像进画面墙换画布；点顶排头像高亮选中
- 弹幕面板（blivedm）：贴底整宽、字号可调、可发送（需登录）

**其它**
- 插件体系（`plugins_user/<名字>/plugin.py`）
- 深色主题、圆角、自绘图标；窗口状态与布局按横/竖屏分别记住

### 更现代化的 UI

- **一整块画面墙当家**：关注的直播间一屏排开，一格一路；画面是圆角的，控制按钮鼠标移上去才浮出来，平时干干净净。
- **竖屏也能用**：屏幕竖过来（或竖屏副屏）时，顶上一栏就是关注列表，可以左右滑；主画面按 16:9 占满整行宽度，小画面自动排满下面。
- **深色界面、统一质感**：配色、圆角、字号是一套设计令牌；图标全部手绘，不会出现字体缺符号那种方块字。
- **浮标不挡画面**：LIVE、在线人数、直播间标题默认收起来，鼠标移到格子上才出现；一屏十几路也不会糊成一片。
- **状态一眼能看懂**：在播 / 未开播 / 已下播 / 连接失败 / 缓冲中 / 暂停 / 空位，各有各的显示，不用猜。
- **关注列表卡片化**：头像 + 主播名 + 直播标题 + 开播角标，置顶的卡片带角标；布局菜单用缩略图，一眼看出是几路、怎么排。

### 更自定义的设置

- **每一路都能单独调**：音量、静音、画质（原画 / 蓝光 / 高清 / 低清），互不影响。
- **可以把某一路的声音单独丢到左耳或右耳** —— 想同时听两三路、又不想混在一起的时候特别有用。
- **布局随便挑**：13 套横屏布局（等分的、主画面带小画面的都有）+ 6 套弹幕布局 + 6 套竖屏布局；菜单只列出你当前方向能用的，不会选错。
- **布局会记住，还会自己换**：横屏、竖屏各记一套；把窗口拖成竖的，它会自动切到最接近的竖屏布局，不用重新选。
- **关注列表听你的**：置顶、拖动排序、三种自动排序（自定义 / 开播优先 / 导入顺序）、多选删除、搜索。
- **弹幕面板可调**：保留条数、字号、屏蔽词、字体、粉丝牌、B 站表情。
- **快捷键可以改**：默认 `F` 把鼠标那一路设成主画面、`Esc` 还原上一个布局、`M` 只保留那一路的声音。
- **想加功能就写插件**：放进 `plugins_user/<名字>/plugin.py`，就能多一个格子右键菜单、多支持一个平台或转发弹幕，不用改本体。

### 更流畅的体验

- **开十几路也不卡、不吃内存**：所有画面共用一套播放内核，不用为每一格各起一套。
- **断流不用你管**：网络抖动断流会自动重连（5 秒起、逐次翻倍、最多 60 秒），画面卡死也会自动刷新。
- **主播下播不清场**：格子留着（画面清黑写「已下播」），主播一开播自动接着播；取流失败只显示「连接失败」继续重试，不会误判成下播。
- **换布局、换方向不打断**：切换布局或把窗口拖成竖屏时，正在播的画面不会重连、不闪。
- **列表再长也顺**：头像、封面、表情都在后台加载并缓存在本地，关注列表拉很长也不顿。
- **拿到就能用**：发布页下载一个 zip，解压双击 exe 就跑，不用装 Python、也不用单独装 VLC；想跑源码的话双击 `run.cmd` 即可。

> 还有些原版有、这一版暂时没搬过来的：热榜/热播主播列表、独立悬浮窗、硬解软解切换、全局画质与全局音频、缓存大小设置、开播自动播放、自动更新；另外这版只支持 Windows。录制、发弹幕、接别的直播平台按设计走插件。

## 运行

- **exe 便携版（推荐）**：下载 `DDMonitorCE-v0.1-exe.zip`，**解压后直接双击里面的
  `DD监控室CE-v0.1-exe.exe`** 就能用（不需要装 Python）。配置 / 缓存 / 日志都在解压出来的
  那个目录下（`utils\config.json`、`cache\`、`logs\`），删掉 `utils\config.json` 等于恢复出厂。
  里面的 `_internal\`（含 `libvlc.dll` 和 `plugins\`）是运行库，别删。
  不想解压也行：本地 `results\DD监控室CE-v0.1-exe\` 就是同一个目录。
- **源码便携包**：`results\DD监控室CE-v0.1\` 整个目录拷走 →
  ① 机器上有 Python 3.12/3.13：`python -m venv .venv` →
  `.venv\Scripts\pip install -r requirements.txt` → 双击 `run.cmd`；
  ② 本机已有现成虚拟环境（`F:\CodexAppManager\Code\DD_Monitor-venv`）时
  双击 `run.cmd` 即可，它会自己找到解释器。

## 许可

程序本体见 `LICENSE`；第三方组件（libVLC、PySide6、python-vlc 等）见 `NOTICE.md`。
