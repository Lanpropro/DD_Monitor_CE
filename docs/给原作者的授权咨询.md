# 给 DD监控室 原作者的授权咨询（草稿）

> 用途：发在 Gitee 仓库的 Issue / 私信，或作者留下的联系方式。
> 建议标题：**关于 DD监控室 二次开发（界面重构）的授权咨询**

---

你好，我是 DD监控室 的长期用户，平时就是靠它同时挂好几个直播间。先说一句谢谢，这个软件帮了我很多。

我在自己电脑上按自己的使用习惯重做了一版界面（PySide6 重写界面层，播放仍然走 python-vlc，取流和 B 站接口的调用逻辑参考了原项目的实现），加了一些单路控制（每格暂停、音量、画质、独立弹幕格布局之类）。**目前只在本地自用，没有对外发布，也没有任何商业用途**，所有代码都放在我自己的机器里。

在继续之前想先问一下你的意见，免得做了你不希望的事：

1. 我自己改着用，你介意吗？
2. 如果将来我想把改动整理出来分享（比如放到 Gitee/GitHub，显著位置写明「基于 DD监控室 二次开发」，保留你的名字和仓库里的许可文件），你允许吗？如果可以，你希望我用什么方式署名、是否沿用仓库里的 LGPL-2.1，或者你更希望用哪种许可？
3. 如果你不希望被分发，我就只留作自用，不再外传；我也可以把改动整理成补丁/PR 发给你，你要是觉得有用就直接合并到主仓库，署名按你的意思来。
4. 顺便确认一个小事：仓库根目录的 LICENSE 是 **LGPL-2.1**，这是你有意选的吗？（应用类项目用 LGPL 的情况比较少见，我想确认后按你的意思处理，而不是自作主张。）

另外两条可能对你有用的小情报，顺手提一下：

- 仓库里 `blivedm/`（弹幕库）没有附带它上游的许可文件，如果以后要重新启用弹幕功能，可能需要补一下；
- 我在改的时候遇到过关窗瞬间偶发崩溃（后台网络线程还在跑时 Qt/VLC 析构），现在的做法是关窗后直接退出进程，如果你也遇到过可以试试。

不管结果如何都谢谢你花时间看完，也谢谢你愿意把代码开源。期待你的回复。

——（你的昵称 / 联系方式）

---

## 附：可以随信附上的改动清单（让对方一眼看清范围）

- 界面层全部重写：左侧关注列表（可收起、可拖动排序、置顶）、画面墙布局系统（普通布局 + 独立弹幕布局两组）
- 单路控制：每格画质 / 音量 / 暂停 / 刷新 / 关闭，主画面自动原画、其余 720P
- 登录与导入：扫码登录 + 内置浏览器账号密码登录，导入关注（多选、只挑近两周有开播记录的）
- 取流：直连 http FLV 不落盘，App 通道优先、失败回退 web 通道
- 未包含：热榜、录制、悬浮窗
